# Phase 999.17 — Plan 01 Summary

**Phase:** 999.17-gui-installed-language-pairs-duplicates  
**Plan:** 01  
**Status:** Complete  
**Completed:** 2026-04-13

## What Was Built

Fixed duplicate entries in the "Installed Language Pairs" Help menu dialog by adding a seen-set deduplication guard inside `list_installed_pairs()`.

## Changes Made

### `gensubtitles/core/translator.py`
- Replaced the bare `pairs.append()` loop in `list_installed_pairs()` with a `seen: set[tuple[str, str]]` guard
- Each `(lang.code, t.to_lang.code)` key is checked before appending — duplicates skipped
- No callers changed; fix propagates automatically to `GET /languages` and GUI dialog

### `tests/test_translator.py`
- Added `test_list_installed_pairs_deduplicates()` regression test
- Mocks two language objects with identical `"en"` codes both having `"es"` translation
- Asserts exactly one `{"from": "en", "to": "es"}` entry in the result

## Verification

```
2 passed in 0.25s
- test_list_installed_pairs_deduplicates PASSED
- test_list_installed_pairs_returns_dicts PASSED
```

Full suite: 43 passed, 2 pre-existing Windows temp-file PermissionError failures (unrelated to this phase).

## Decisions Honored

- **D-01:** Fix applied only in `list_installed_pairs()` — single source of truth
- **D-02:** No defensive dedup added to API router or GUI dialog
- **D-03:** Regression test added in `tests/test_translator.py`

## Artifacts

- `gensubtitles/core/translator.py` — patched with seen-set guard
- `tests/test_translator.py` — regression test added
- Commit: `fix(999.17): deduplicate list_installed_pairs() with seen-set guard`
