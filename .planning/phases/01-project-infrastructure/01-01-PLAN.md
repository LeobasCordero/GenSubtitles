---
phase: 01-project-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - .gitignore
  - models/.gitkeep
  - temp/.gitkeep
  - output/.gitkeep
autonomous: true
requirements:
  - INF-01
  - INF-04

must_haves:
  truths:
    - "pyproject.toml exists with [project] table, requires-python = '>=3.11', all 7 runtime deps, pytest in dev extras, and gensubtitles entry point"
    - "requirements.txt lists all 7 packages with >= pins and includes FFmpeg system-dependency comment block"
    - ".gitignore covers models/, temp/, output/, __pycache__, *.pyc, .env, .venv/, *.egg-info/"
    - "models/, temp/, output/ directories exist at project root with .gitkeep placeholders"
  artifacts:
    - path: "pyproject.toml"
      provides: "Authoritative project metadata, deps, entry point, pytest config"
      contains: "requires-python"
    - path: "requirements.txt"
      provides: "pip fallback install list with >= pins"
      contains: "faster-whisper"
    - path: ".gitignore"
      provides: "Prevents committing build artifacts, venvs, secrets"
      contains: "models/"
    - path: "models/.gitkeep"
      provides: "models/ directory in VCS"
    - path: "temp/.gitkeep"
      provides: "temp/ directory in VCS"
    - path: "output/.gitkeep"
      provides: "output/ directory in VCS"
  key_links:
    - from: "requirements.txt"
      to: "pyproject.toml [project.dependencies]"
      via: "manually kept in sync"
      pattern: "faster-whisper>=1.2.1"
    - from: ".gitignore"
      to: "models/, temp/, output/"
      via: "gitignore pattern"
      pattern: "^models/"
---

<objective>
Create the authoritative project config files: `pyproject.toml` (hatchling build, all deps, entry point, pytest config), `requirements.txt` (pip fallback with >= pins), `.gitignore` (covers build artifacts, venvs, runtime dirs), and placeholder directories (`models/`, `temp/`, `output/`).

Purpose: Per D-06, pyproject.toml is authoritative for metadata and deps. requirements.txt is the documented pip-only fallback (D-04). .gitignore + placeholders per D-15.
Output: pyproject.toml, requirements.txt, .gitignore, models/.gitkeep, temp/.gitkeep, output/.gitkeep
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-infrastructure/01-CONTEXT.md
@.planning/phases/01-project-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/01-project-infrastructure/01-RESEARCH.md (Pattern 1: pyproject.toml for uv — exact toml content)
    - .planning/phases/01-project-infrastructure/01-CONTEXT.md (D-06: pyproject.toml is authoritative; D-07: no Poetry/pip-tools; D-08: requires-python >=3.11)
    - .planning/phases/01-project-infrastructure/01-VALIDATION.md (Wave 0 requirements: [tool.pytest.ini_options] section with testpaths=["tests"])
  </read_first>
  <action>
Create `pyproject.toml` at project root with EXACTLY this content (from RESEARCH.md Pattern 1, extended with pytest config):

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gensubtitles"
version = "0.1.0"
description = "Automatic video subtitle generation — offline, no API keys required"
requires-python = ">=3.11"
dependencies = [
    "faster-whisper>=1.2.1",
    "argostranslate>=1.11.0",
    "srt>=3.5.3",
    "fastapi>=0.135.3",
    "uvicorn[standard]>=0.34.0",
    "python-multipart>=0.0.20",
    "typer[all]>=0.15.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]

[project.scripts]
gensubtitles = "gensubtitles.cli.main:app"

[tool.hatch.build.targets.wheel]
packages = ["gensubtitles"]

[tool.pytest.ini_options]
testpaths = ["tests"]
```

No deviations — use this verbatim. The `[tool.pytest.ini_options]` section is required by VALIDATION.md Wave 0.
  </action>
  <verify>
    <automated>python -c "import tomllib; data=tomllib.load(open('pyproject.toml','rb')); assert data['project']['requires-python']=='>=3.11'; assert 'faster-whisper>=1.2.1' in data['project']['dependencies']; assert data['project']['scripts']['gensubtitles']=='gensubtitles.cli.main:app'; print('pyproject.toml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists at project root
    - `grep "requires-python" pyproject.toml` returns `requires-python = ">=3.11"`
    - `grep "faster-whisper" pyproject.toml` returns `"faster-whisper>=1.2.1",`
    - `grep "hatchling" pyproject.toml` returns build-backend line
    - `grep "gensubtitles.cli.main:app" pyproject.toml` returns the scripts entry
    - `grep "testpaths" pyproject.toml` returns `testpaths = ["tests"]`
    - `grep "pytest>=8.0.0" pyproject.toml` returns dev dependency line
  </acceptance_criteria>
  <done>pyproject.toml exists with all 7 runtime deps, hatchling build system, gensubtitles entry point, dev pytest dep, and pytest testpaths config.</done>
</task>

<task type="auto">
  <name>Task 2: Create requirements.txt, .gitignore, and placeholder directories</name>
  <files>requirements.txt, .gitignore, models/.gitkeep, temp/.gitkeep, output/.gitkeep</files>
  <read_first>
    - requirements.txt (existing file — currently empty; overwrite it)
    - .planning/phases/01-project-infrastructure/01-RESEARCH.md (Pattern 2: exact requirements.txt content with FFmpeg comment block; Common Pitfalls §2: don't use uv pip compile)
    - .planning/phases/01-project-infrastructure/01-CONTEXT.md (D-01: >= versions; D-03: requirements.txt is pip fallback; D-04: document pip install as fallback; D-15: models/, temp/, output/ with .gitkeep)
  </read_first>
  <action>
**1. Overwrite `requirements.txt`** with EXACTLY this content (from RESEARCH.md Pattern 2):

```
# Install: pip install -r requirements.txt
# Primary toolchain: uv sync (see pyproject.toml)
#
# SYSTEM DEPENDENCY: FFmpeg must be installed separately
# Linux:   sudo apt install ffmpeg
# macOS:   brew install ffmpeg
# Windows: winget install ffmpeg

faster-whisper>=1.2.1
argostranslate>=1.11.0
srt>=3.5.3
fastapi>=0.135.3
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
typer[all]>=0.15.0
```

**2. Create `.gitignore`** at project root with this content:

```
# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Environment variables
.env
.env.*

# Runtime directories (kept via .gitkeep, contents ignored)
models/*
!models/.gitkeep
temp/*
!temp/.gitkeep
output/*
!output/.gitkeep

# uv
.uv/

# OS
.DS_Store
Thumbs.db
```

**3. Create placeholder directories** by creating an empty `.gitkeep` file in each:
- `models/.gitkeep` — empty file
- `temp/.gitkeep` — empty file
- `output/.gitkeep` — empty file

These directories are gitignored EXCEPT for .gitkeep so the directories are tracked in VCS.
  </action>
  <verify>
    <automated>python -c "
import os
# requirements.txt
with open('requirements.txt') as f: content = f.read()
assert 'faster-whisper>=1.2.1' in content, 'Missing faster-whisper'
assert 'uvicorn[standard]>=0.34.0' in content, 'Missing uvicorn'
assert 'typer[all]>=0.15.0' in content, 'Missing typer'
assert 'sudo apt install ffmpeg' in content, 'Missing FFmpeg Linux note'
assert 'winget install ffmpeg' in content, 'Missing FFmpeg Windows note'
# .gitignore
with open('.gitignore') as f: gi = f.read()
assert 'models/*' in gi, 'models/ not gitignored'
assert '__pycache__/' in gi, '__pycache__ not gitignored'
# placeholders
for d in ['models/.gitkeep', 'temp/.gitkeep', 'output/.gitkeep']:
    assert os.path.exists(d), f'{d} missing'
print('requirements.txt, .gitignore, placeholders OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "faster-whisper>=1.2.1" requirements.txt` exits 0
    - `grep "uvicorn\[standard\]>=0.34.0" requirements.txt` exits 0
    - `grep "typer\[all\]>=0.15.0" requirements.txt` exits 0
    - `grep "winget install ffmpeg" requirements.txt` exits 0 (INF-04 FFmpeg Windows note)
    - `grep "brew install ffmpeg" requirements.txt` exits 0 (INF-04 FFmpeg macOS note)
    - `.gitignore` contains `models/*` and `!models/.gitkeep`
    - `.gitignore` contains `__pycache__/`
    - `.gitignore` contains `.venv/`
    - `models/.gitkeep`, `temp/.gitkeep`, `output/.gitkeep` all exist as files
  </acceptance_criteria>
  <done>requirements.txt has 7 packages with >= pins and FFmpeg system-dep comment block; .gitignore covers all runtime dirs, pycache, venvs, .env; models/, temp/, output/ exist with .gitkeep placeholders.</done>
</task>

</tasks>

<verification>
- `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0 (valid TOML)
- `grep "requires-python" pyproject.toml` shows `">=3.11"`
- `grep "faster-whisper" requirements.txt` shows `>=1.2.1`
- `grep "winget" requirements.txt` shows FFmpeg Windows install note
- All 3 `.gitkeep` files exist
</verification>

<success_criteria>
`pyproject.toml` is a valid TOML with hatchling build system, 7 runtime deps with >= constraints, pytest in dev extras, gensubtitles entry point, and testpaths config. `requirements.txt` mirrors deps with >= pins and FFmpeg comment block. `.gitignore` covers runtime dirs and Python artifacts. Placeholder dirs exist.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-infrastructure/01-01-SUMMARY.md`
</output>
