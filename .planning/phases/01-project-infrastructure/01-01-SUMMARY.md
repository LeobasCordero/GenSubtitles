---
phase: 01-project-infrastructure
plan: 01
subsystem: infra
tags: [pyproject, hatchling, uv, requirements, gitignore, python-packaging]

requires: []
provides:
  - "pyproject.toml: authoritative project metadata, 7 runtime deps, hatchling build, gensubtitles entry point, pytest config"
  - "requirements.txt: pip fallback install list with >= pins and FFmpeg system dep comment"
  - ".gitignore: covers models/, temp/, output/, __pycache__, venvs, build artifacts, secrets"
  - "models/, temp/, output/ directories tracked via .gitkeep placeholders"
affects: [all phases — every phase installs deps via pyproject.toml / requirements.txt]

tech-stack:
  added: [hatchling, faster-whisper>=1.2.1, argostranslate>=1.11.0, srt>=3.5.3, fastapi>=0.135.3, uvicorn[standard]>=0.34.0, python-multipart>=0.0.20, typer[all]>=0.15.0, pytest>=8.0.0]
  patterns: ["pyproject.toml as authoritative dep source (D-06)", "requirements.txt as pip fallback (D-04)", ".gitkeep for empty dirs in VCS (D-15)"]

key-files:
  created: [pyproject.toml, models/.gitkeep, temp/.gitkeep, output/.gitkeep]
  modified: [requirements.txt, .gitignore]

key-decisions:
  - "pyproject.toml with hatchling is authoritative for metadata and deps (D-06)"
  - "requirements.txt is manually maintained pip fallback, not generated by uv (D-03, D-04)"
  - "models/, temp/, output/ tracked via .gitkeep with contents gitignored (D-15)"

patterns-established:
  - "Pattern: pyproject.toml [project.dependencies] with >= minimum version pins"
  - "Pattern: .gitignore uses dir/* + !dir/.gitkeep to track empty dirs"

requirements-completed: [INF-01, INF-04]

duration: 5min
completed: 2026-04-02
---

# Phase 1 Plan 1: Project Config Files Summary

**pyproject.toml (hatchling build, 7 deps, entry point) + requirements.txt pip fallback + .gitignore + placeholder runtime directories**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-02T00:00:00Z
- **Completed:** 2026-04-02T00:05:00Z
- **Tasks:** 2 completed
- **Files modified:** 5 (created: 4, modified: 2 existing)

## Accomplishments

- Created `pyproject.toml` with hatchling build system, all 7 runtime deps with >= pins, `gensubtitles` CLI entry point, pytest dev extra, and `[tool.pytest.ini_options]` config
- Populated `requirements.txt` as documented pip fallback with FFmpeg system dependency comment block
- Updated `.gitignore` to cover runtime directories (`models/`, `temp/`, `output/`) with .gitkeep exceptions
- Created `models/`, `temp/`, `output/` directories with `.gitkeep` placeholders for VCS tracking

## Task Commits

1. **Task 1: Create pyproject.toml** — `df5871e` (chore)
2. **Task 2: requirements.txt, .gitignore, placeholder dirs** — `f1e086b` (chore)

## Files Created/Modified

- `pyproject.toml` — authoritative project metadata, build system, deps, entry point, pytest config
- `requirements.txt` — pip fallback with >= pins and FFmpeg install instructions comment
- `.gitignore` — added models/*, temp/*, output/* patterns with .gitkeep exceptions
- `models/.gitkeep` — tracks models/ directory in VCS
- `temp/.gitkeep` — tracks temp/ directory in VCS
- `output/.gitkeep` — tracks output/ directory in VCS

## Decisions Made

- Used `models/*` + `!models/.gitkeep` gitignore pattern (rather than bare `models/`) so the directory is tracked but contents are ignored.
- requirements.txt manually maintained as pip fallback per D-03/D-04 — not generated by `uv pip compile`.

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- `pyproject.toml` — FOUND
- `requirements.txt` — FOUND (contains faster-whisper>=1.2.1, FFmpeg comment)
- `.gitignore` — FOUND (contains models/, temp/, output/)
- `models/.gitkeep` — FOUND
- `temp/.gitkeep` — FOUND
- `output/.gitkeep` — FOUND
- commit `df5871e` — FOUND
- commit `f1e086b` — FOUND
