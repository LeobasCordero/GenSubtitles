---
plan: 999.14-03
phase: 999.14-gui-api-http-timeout-during-subtitle-generation
status: complete
completed: 2026-04-13
---

## Summary

Added `TestSSEJobPattern` to `tests/test_api.py` (6 tests) and `TestPipelineCancellation` to `tests/test_pipeline.py` (2 tests). Retired `TestPostSubtitles` and `TestGetProgress` classes (they tested now-removed endpoints). Updated `test_openapi_json_includes_both_endpoints` to check for the new SSE endpoint paths.

## Key Files

### Modified
- `tests/test_api.py` — Removed `TestPostSubtitles` and `TestGetProgress` (tested retired endpoints). Fixed `test_openapi_json_includes_both_endpoints`. Added `TestSSEJobPattern` with 6 tests using existing `client` fixture.
- `tests/test_pipeline.py` — Appended `TestPipelineCancellation` with 2 tests for cancel check-points in `_run_pipeline_job`.

## Tests Added

### TestSSEJobPattern (test_api.py)
| Test | Verifies |
|------|---------|
| `test_post_subtitles_async_returns_job_id_immediately` | POST returns UUID job_id with 200 |
| `test_stream_yields_done_event` | GET /stream yields SSE events ending with stage=done |
| `test_get_result_returns_409_before_done` | GET /result returns 409 before pipeline finishes |
| `test_get_result_returns_srt_after_done` | GET /result returns 200 + SRT content after done |
| `test_delete_sets_cancel_flag` | DELETE sets cancel threading.Event |
| `test_unsupported_extension_rejected` | POST rejects non-video with 400 |

### TestPipelineCancellation (test_pipeline.py)
| Test | Verifies |
|------|---------|
| `test_cancel_after_extract_stops_pipeline` | Cancel after extract stops before transcription |
| `test_cancel_not_set_allows_full_pipeline` | No cancel → done event emitted |

## Verification

```
24 passed in 5.46s
```

All 24 tests pass. No regressions.

## Self-Check: PASSED
