---
plan: 999.10-03
phase: 999.10-feature-expansion-language-formats-packaging-settings-help
status: complete
commit: 89f4bc5
---

# Summary: 999.10-03 — Dynamic Language Dropdowns + Output Format Selector

## What Was Built

1. **Replaced `_LANG_MAP` with `_CODE_TO_LABEL` + `_label_to_code()` helper**
   - Module-level dict mapping ISO codes to display names (15 languages)
   - Helper function for reverse label→code lookup

2. **Dynamic source language dropdown (re-enabled)**
   - Replaced hidden `CTkEntry` with `CTkOptionMenu` at row 2
   - Populated with "Auto-detect" + installed sources after server starts
   - `_on_source_lang_change()` cascades target dropdown to valid pairs

3. **Dynamic target language dropdown**
   - Drops "Spanish/English/Other" static values
   - Populated from `GET /languages`; filters based on selected source

4. **Output format dropdown (row 4)**
   - SRT / SSA options; `_on_output_format_change()` updates output path extension
   - Browse dialogs use correct extension per selection
   - `_run_api_call()` converts SRT→SSA post-generation when SSA selected

5. **Server-ready language loading**
   - `_start_server()` spawns a background thread polling `GET /languages`
   - On success, calls `self.after(0, self._populate_language_dropdowns)`

## Key Files

key-files:
  modified:
    - gensubtitles/gui/main.py

## Verification

- `import gensubtitles.gui.main` → OK
- All 4 new methods/attributes found → PASSED

## Self-Check: PASSED
