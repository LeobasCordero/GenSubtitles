---
plan: 999.10-06
phase: 999.10-feature-expansion-language-formats-packaging-settings-help
status: complete
commit: 16d18ca
---

# Summary: 999.10-06 — Help Menu Dialogs

## What Was Built

1. **`_show_tutorial()`** — CTkToplevel with CTkScrollableFrame; multi-section usage guide covering Generate tab, Translate tab, language model installation, Settings, and Troubleshooting

2. **`_show_language_pairs()`** — CTkToplevel listing `self._language_pairs` with human-readable names in `Source → Target` format; shows "no pairs installed" message when list is empty

3. **`_show_about()`** — CTkToplevel showing app version (`gensubtitles.__version__`), MIT license, GitHub button (opens `webbrowser`), and Close button

All three are non-blocking CTkToplevel windows with `grab_set()` for focus management.

## Human Verification

Checkpoint approved by user — all 7 checks passed:
- Menu bar (Settings + Help) visible ✓
- Tutorial scrollable guide opens ✓
- Available Languages lists pairs / shows empty state ✓
- About dialog shows version, MIT, GitHub link ✓
- Settings panel shows fields; Save persists; Back returns ✓
- Generate tab: source/target dropdowns dynamic + output format ✓
- Translate tab: all fields present; Convert Only disables langs ✓

## Key Files

key-files:
  modified:
    - gensubtitles/gui/main.py

## Self-Check: PASSED
