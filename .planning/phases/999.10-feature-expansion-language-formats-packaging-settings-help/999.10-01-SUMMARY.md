---
plan: 999.10-01
phase: 999.10-feature-expansion-language-formats-packaging-settings-help
status: complete
commit: 7ee02df
---

# Summary: 999.10-01 — SSA Support + Settings Module

## What Was Built

1. **OutputFormat enum + SSA functions in `srt_writer.py`**
   - Added `OutputFormat(str, Enum)` with `SRT` and `SSA` members
   - `write_ssa(segments, output_path)` — writes SSA file via pysubs2
   - `convert_srt_to_ssa(srt_path, ssa_path)` — SRT→SSA conversion
   - `convert_ssa_to_srt(ssa_path, srt_path)` — SSA→SRT conversion

2. **New `gensubtitles/core/settings.py`**
   - `AppSettings` dataclass with `appearance_mode`, `ui_language`, `default_output_dir`, `default_source_lang`
   - `load_settings()` — reads from platformdirs user config dir, returns defaults if missing/corrupt
   - `save_settings(settings)` — persists to JSON
   - `settings_path()` — returns resolved path for diagnostics

3. **Dependencies added** to `pyproject.toml` and `requirements.txt`
   - `pysubs2>=1.6.0`
   - `platformdirs>=4.0.0`

## Key Files

key-files:
  created:
    - gensubtitles/core/settings.py
  modified:
    - gensubtitles/core/srt_writer.py
    - pyproject.toml
    - requirements.txt

## Verification

- `from gensubtitles.core.srt_writer import OutputFormat, write_ssa, convert_srt_to_ssa, convert_ssa_to_srt` → OK
- Settings round-trip (save Dark, reload → Dark) → OK
- `settings_path()` → `C:\Users\leocg\AppData\Local\GenSubtitles\GenSubtitles\settings.json`

## Self-Check: PASSED
