---
plan: 999.27-01
phase: 999.27-stepper-mode-for-pipeline-steps
status: complete
commit: 920b2a9
---

## Summary

Created `gensubtitles/core/steps.py` — the shared foundation for stepper mode across CLI, API, and GUI.

## What Was Built

### gensubtitles/core/steps.py
- **4 exported step functions**: `extract_audio_step`, `transcribe_step`, `translate_step`, `write_srt_step`
- **2 JSON helpers**: `segments_to_json` (with optional metadata), `segments_from_json` (handles both dict and list formats)
- **4 filename constants**: `AUDIO_FILENAME`, `TRANSCRIPTION_FILENAME`, `TRANSLATION_FILENAME`, `SRT_FILENAME`
- Lazy imports in all step functions (no import-time FFmpeg/model side effects)
- `transcription.json` written in dict format: `{"language": str, "duration": float, "segments": [...]}`
- `translation.json` written as plain list: `[{start, end, text}, ...]`
- `segments_from_json` handles both formats (backward compatible)

### tests/test_steps.py
- 14 tests covering all step functions, JSON serialization, and edge cases
- All tests pass with no GPU, FFmpeg, or model downloads (all mocked)

## key-files

### created
- gensubtitles/core/steps.py
- tests/test_steps.py

## Verification Results

- `pytest tests/test_steps.py -v`: 14 passed
- Import check: `from gensubtitles.core.steps import ...` OK
- Pre-existing failure `test_cli.py::test_nonexistent_file_exits_one` confirmed unrelated (fails on clean checkout too)

## Self-Check: PASSED
