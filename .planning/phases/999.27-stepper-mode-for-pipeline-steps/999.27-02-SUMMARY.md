---
plan: 999.27-02
phase: 999.27-stepper-mode-for-pipeline-steps
status: complete
commit: a2282b9
---

## Summary

Added `--step` and `--work-dir` flags to the Typer CLI `generate` command, enabling single-stage pipeline execution from the command line.

## What Was Built

### gensubtitles/cli/main.py
- Added `--step` flag with `click.Choice(["extract", "transcribe", "translate", "write"])` validation
- Added `--work-dir` flag for specifying the intermediate artefacts directory
- Added step dispatch block that routes to the 4 step functions from `steps.py`
- Validation: `--work-dir` required when `--step` used (exits 2)
- Validation: `--input` required for `--step extract` (exits 2)
- Validation: `--target-lang` required for `--step translate` (exits 2)
- Full-run path (`run_pipeline`) untouched

### tests/test_cli.py
- 7 new step-mode test functions appended
- All mock `gensubtitles.core.steps.*_step` at module level to avoid lazy import issues

## key-files

### modified
- gensubtitles/cli/main.py
- tests/test_cli.py

## Verification Results

- `pytest tests/test_cli.py -k step`: 7 passed
- `--step` and `--work-dir` appear in `gensubtitles --help`
- `test_cli.py::test_nonexistent_file_exits_one` pre-existing failure (unrelated)

## Self-Check: PASSED
