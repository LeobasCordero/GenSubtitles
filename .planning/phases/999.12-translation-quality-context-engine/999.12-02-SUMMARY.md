---
plan: 999.12-02
phase: 999.12-translation-quality-context-engine
status: complete
completed: 2026-04-11
---

## Summary

Exposed the engine parameter across CLI, pipeline, and API layers.

## What Was Built

**Task 1: pipeline.py + CLI**
- Added `engine: str = "argos"` parameter to `run_pipeline()`
- Forwarded `engine=engine` to `translate_segments()` in Stage 3
- Added `--engine [argos|deepl|libretranslate]` CLI option to `generate()` using `click.Choice`
- CLI passes `engine=engine` to `run_pipeline()`

**Task 2: API router**
- Added `engine: str = Query(default="argos", pattern="^(argos|deepl|libretranslate)$")` to `post_subtitles()`
- Forwarded `engine=engine` to `translate_segments()` in the translation block

## Key Files

key-files.created:
- gensubtitles/core/pipeline.py — engine param added to run_pipeline()
- gensubtitles/cli/main.py — --engine flag added to generate()
- gensubtitles/api/routers/subtitles.py — engine Query param added

## Test Results

41 tests passed (test_cli.py + test_pipeline.py + test_api.py)

## Deviations

- Updated `tests/test_pipeline.py::test_translation_runs_when_target_lang_provided` to include `engine="argos"` in the expected call (the call now explicitly passes engine)

## Self-Check: PASSED
