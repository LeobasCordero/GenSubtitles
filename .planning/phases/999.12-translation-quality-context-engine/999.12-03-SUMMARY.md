---
plan: 999.12-03
phase: 999.12-translation-quality-context-engine
status: complete
completed: 2026-04-11
---

## Summary

Added engine selection CTkOptionMenu to the GUI Generate Subtitles tab.

## What Was Built

**Task 1: Engine dropdown — build, show/hide, disable/enable**
- Added `self._engine_var = ctk.StringVar(value="Argos")` in `__init__`
- Added `_lbl_engine` + `_option_engine` CTkOptionMenu at row 4 (values: Argos/DeepL/LibreTranslate)
- Both widgets hidden initially via `grid_remove()`
- Shifted all subsequent rows: output format→5, generate→6, clear→7, elapsed→8, progress→9, stage→10
- `_on_target_lang_change()` shows engine row when target != "No target", hides it otherwise
- `_on_generate()` disables `_option_engine` alongside other widgets
- `_finish_generate()` re-enables `_option_engine`

**Task 2: Wire to API call**
- `_on_generate()` reads `_engine_var.get()` and converts label to API code (e.g., "LibreTranslate"→"libretranslate")
- `engine_code` passed as 5th arg to `_run_api_call()`
- `_run_api_call()` signature updated with `engine: str = "argos"` param
- `params["engine"] = engine` always included in request params

## Key Files

key-files.created:
- gensubtitles/gui/main.py — engine dropdown + lifecycle + API wiring

## Test Results

Syntax check passed. No GUI-specific automated tests (GUI is headless).

## Self-Check: PASSED
