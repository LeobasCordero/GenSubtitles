# Phase 999.12: Translation Quality — Context-Aware & Engine Upgrade - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

---

## Area 1: Batched Translation Mechanics

**Q: How should segments be concatenated for batch translation?**

Options presented:
- `\n` (newline separator)
- `\n---\n` (triple-dash delimiter)
- **Numbered XML markers `<1>text</1><2>text</2>…`** ← Selected

**Q: What if the split-back count doesn't match the original segment count?**

Options presented:
- A. Warn + fall back to per-segment translation
- **B. Raise RuntimeError immediately** ← Selected
- C. Best-effort alignment

**Q: For multi-hop pivot routes, should batching apply to each hop?**

Options presented:
- **1. Batch every hop** ← Selected
- 2. Batch only direct pairs; per-segment fallback for pivot hops
- 3. Agent's discretion

---

## Area 2: Engine Upgrade Scope

**Q: What is in scope for this phase?**

Options presented:
- 1. Context-batching only (no alt engines)
- 2. DeepL only
- **3. DeepL Free API + LibreTranslate (self-hosted)** ← Selected
- 4. DeepL + LibreTranslate as pluggable engine abstraction

---

## Area 3: Configuration Surface

**Q: Where should engine selection live?**

Options presented:
- **1. CLI flag + API param per-run only; no persistent default** ← Selected
- 2. AppSettings persistent preference + CLI/API override
- 3. CLI/API only; GUI uses AppSettings default

**Q: Where should the GUI engine selector appear?**

Options presented:
- 1. Inline on main form, always visible
- **2. Only visible when a target language is selected** ← Selected
- 3. Agent's discretion

---

## Area 4: Fallback Behavior & Credential Storage

**Q: Where do credentials live?**

Options presented:
- 1. Environment variables only
- **2. settings.json (AppSettings fields)** ← Selected
- 3. Env vars first, settings.json fallback

**Q: What happens on missing or failed credentials?**

Options presented:
- **A. Raise immediately with a descriptive error message** ← Selected
- B. Auto-fall back to Argos silently
- C. Auto-fall back to Argos with a warning log

---

*Discussion conducted: 2026-04-11*
