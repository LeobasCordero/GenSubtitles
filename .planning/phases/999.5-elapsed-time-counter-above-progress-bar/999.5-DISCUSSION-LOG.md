# Phase 999.5: GUI — Elapsed Time Counter Above Progress Bar - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-10
**Phase:** 999.5-elapsed-time-counter-above-progress-bar
**Areas discussed:** Visibility at rest, Post-completion behavior, Clear/next run reset

---

## Visibility at Rest

| Option | Description | Selected |
|--------|-------------|----------|
| Hidden | Appears only when generation starts (like the progress bar) | ✓ |
| Always visible | Shows `00:00:00` at rest as a ready-state indicator | |

**User's choice:** Hidden  
**Notes:** Consistent with the progress bar pattern — widget exists in the grid but is hidden via `.grid_remove()` until generation begins.

---

## Post-Completion Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Lingers | Final elapsed time stays visible after generation finishes | ✓ |
| Disappears | Hides together with the progress bar, symmetric cleanup | |

**User's choice:** Lingers  
**Notes:** User wants to see how long the job took. Counter remains visible after `_finish_generate` is called. Progress bar still disappears as before.

---

## Clear Button / Next Run Reset

| Option | Description | Selected |
|--------|-------------|----------|
| Next run resets | Counter only resets/hides when next generation starts; Clear leaves it as-is | ✓ |
| Clear button hides it | Clicking Clear also hides the counter, symmetric with form reset | |

**User's choice:** Next run resets  
**Notes:** The lingering final time is not cleared by the Clear button. When the user starts a new generation, `_on_generate` resets to `00:00:00` and begins counting again.

---

## Agent's Discretion

- Exact naming of timer handle and start-time instance attributes
- `time.monotonic()` vs `time.time()` for elapsed tracking
- Counter update interval
- Exact grid row numbers after inserting the new row

## Deferred Ideas

None.
