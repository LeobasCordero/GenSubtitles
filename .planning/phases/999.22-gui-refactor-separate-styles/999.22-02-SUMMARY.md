# 999.22-02 — SUMMARY

## What Was Built

Updated `gensubtitles/gui/main.py` to consume `styles.py` using the
apply-after-construction pattern (D-04), replaced magic numbers with constants,
and slimmed down `_apply_theme()` to call helper functions (D-03).

## Key Files

### Modified
- `gensubtitles/gui/main.py` — imports styles, widget construction refactored

## Changes Made

### Import
- Added `from .styles import (...)` covering all constants and helpers (line 22)

### _build_ui() — 7 widgets updated
- `_entry_input`, `_entry_output` → `apply_entry_style()`
- `_btn_generate` → `apply_accent_btn_style()`, `height=BTN_HEIGHT_PRIMARY`
- `_btn_clear` → `apply_secondary_btn_style()`, `height=BTN_HEIGHT_PRIMARY`
- `_progress_bar` → `apply_progress_bar_style()`, `height=PROGRESS_BAR_HEIGHT`
- `_stage_label` → `apply_stage_label_style()`
- `_btn_cancel` → `apply_cancel_btn_style()`, `height=BTN_HEIGHT_CANCEL`
- Browse/Save buttons → `width=BTN_WIDTH_BROWSE`

### _build_settings_panel() — 9 widgets updated
- `_settings_header_lbl` → `apply_settings_header_style()` (font + text_color)
- `_settings_outdir_entry` → `apply_entry_style()`
- `_lbl_subtitle_style` → `apply_secondary_label_style()`
- `_settings_font_size_entry` → `apply_entry_style()`, `width=BTN_WIDTH_BROWSE`
- `_btn_text_color_swatch`, `_btn_outline_color_swatch` → `width=BTN_WIDTH_SWATCH`, `height=BTN_HEIGHT_MINI`
- `_lbl_config_path_label`, `_lbl_config_path_value` → `apply_secondary_label_style()`
- `_btn_open_config_folder` → `apply_secondary_btn_style()`, `width=BTN_WIDTH_NARROW`
- `_btn_settings_save` → `apply_accent_btn_style()`, `height=BTN_HEIGHT_PRIMARY`
- `_btn_settings_back` → `apply_secondary_btn_style()`, `height=BTN_HEIGHT_PRIMARY`

### _build_translate_tab() — 5 widgets updated
- `_tl_entry_input`, `_tl_entry_output` → `apply_entry_style()`
- `_tl_btn_translate` → `apply_accent_btn_style()`, `height=BTN_HEIGHT_PRIMARY`
- `_tl_progress_bar` → `apply_progress_bar_style()`, `height=PROGRESS_BAR_HEIGHT`
- Browse/Save buttons → `width=BTN_WIDTH_BROWSE`

### _apply_theme() — all inline configure() calls replaced with helpers
- `self.configure(fg_color=p("bg"))` → `apply_window_bg(self)`
- Entry loop → `apply_entry_style(entry)`
- Accent btn loop → `apply_accent_btn_style(btn)`
- `_btn_clear` → `apply_secondary_btn_style()`
- Progress bar loop → `apply_progress_bar_style(pb)` (keeps mode check)
- Settings conditional blocks use corresponding helpers

## Self-Check: PASSED
