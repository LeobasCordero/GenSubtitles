# Phase 999.2: GUI — Clear Fields Button - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-10
**Phase:** 999.2 — GUI Clear Fields Button

---

## Area 1: What "clear" resets

**Q:** Does it clear all 4 fields + hide the "Open Folder" button + reset the stage label? Or only the 4 input fields?

Options presented:
- Input fields only — just the 4 StringVars; stage label and Open Folder button stay visible
- Full UI reset — input fields + hide Open Folder + clear stage label

**Selected:** Input fields only

---

## Area 2: Button placement

**Q:** Where in the grid should the Clear button go?

Options presented:
- Same row as Generate, side by side
- Its own row below Generate

**Selected:** Its own row below Generate

**Follow-up clarification:** User specified the button must only be available (enabled) when at least one input field contains text.

---

## Area 3: Behavior during pipeline

**Q:** Should Clear be disabled while generation is running?

Options presented:
- Yes, disable during pipeline
- No, leave it enabled

**Selected:** Yes, disabled during pipeline

---

## Area 4: Reset values

**Q:** What value does each field reset to — all empty or partial defaults?

Options presented:
- All empty "" — placeholder text reappears
- Partial defaults — source language resets to "en", others empty

**Selected:** All empty ""
