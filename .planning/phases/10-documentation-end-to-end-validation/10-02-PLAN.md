---
phase: 10-documentation-end-to-end-validation
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified: [README.es.md]
autonomous: true
requirements: [INF-03]

must_haves:
  truths:
    - "Spanish-speaking developer can install following README.es.md only"
    - "Spanish README is complete translation, not abbreviated"
    - "Code examples work identically to English version"
  artifacts:
    - path: "README.es.md"
      provides: "Complete Spanish documentation"
      min_lines: 180
      contains: "## Instalación"
    - path: "README.es.md"
      provides: "Spanish CLI examples"
      contains: "```bash"
  key_links:
    - from: "README.es.md"
      to: "README.md"
      via: "Content parity"
      pattern: "same structure, same code blocks, translated prose"
---

# Plan 10-02: Write README.es.md (Spanish Documentation)

## Objective

Translate README.md to Spanish as a complete standalone file. All prose translated, all code examples preserved byte-for-byte, all section structure maintained.

Purpose: Provide bilingual documentation per D-06 decision (two separate files).
Output: README.es.md at project root (~200 lines, matching README.md structure)

## Execution Context

@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md

## Context

@.planning/phases/10-documentation-end-to-end-validation/10-CONTEXT.md
@.planning/phases/10-documentation-end-to-end-validation/10-01-SUMMARY.md

## Tasks

<task type="auto" tdd="false">
  <name>Task 1: Translate README.md to Spanish</name>
  
  <read_first>
    - README.md (completed in Plan 10-01 — source for translation)
    - .planning/phases/10-documentation-end-to-end-validation/10-CONTEXT.md (D-06: bilingual as two separate files)
  </read_first>
  
  <files>README.es.md</files>
  
  <action>
Create README.es.md as a complete Spanish translation of README.md with these guidelines:

**Translation scope:**
- ALL prose text translated to Spanish (headings, paragraphs, instructions)
- ALL code blocks preserved EXACTLY as-is (bash, curl, Python — no translation)
- ALL command examples byte-for-byte identical (paths, flags, URLs remain in English)
- Section structure identical to README.md

**Section-by-section translation:**

**# GenSubtitles**
→ Brief intro translated

**## Installation** → **## Instalación**
Translate prose explaining FFmpeg and Python requirements.
Keep platform-specific commands unchanged:
```bash
sudo apt install ffmpeg       # Keep as-is
brew install ffmpeg           # Keep as-is
winget install ffmpeg         # Keep as-is
```

**## CLI Usage** → **## Uso de CLI**
Translate explanatory text.
Keep all 4 bash examples unchanged:
```bash
python main.py --input video.mp4              # No translation
python main.py --input video.mp4 --output subtitles.srt
python main.py --input video.mp4 --target-lang es
python main.py --input video.mp4 --model base --device cpu
```

Translate flag descriptions. Flag names stay in English (`--input`, `--output`, etc.).

**## API Usage** → **## Uso de API**
Translate server startup explanation.
Keep curl examples unchanged:
```bash
curl -X POST http://localhost:8000/subtitles ...  # Keep as-is
curl http://localhost:8000/languages               # Keep as-is
```

**## Language Translation** → **## Traducción de Idiomas**
Translate Argos download behavior explanation.

**## Troubleshooting** → **## Solución de Problemas**
Translate the 3 failure mode descriptions and solutions.
Keep error messages in English (they're from the Python code):
```
Error: `EnvironmentError: FFmpeg not found in PATH`  # Keep as-is
```

**## License** → **## Licencia**
MIT (no translation needed)

**Quality checks:**
- Every heading translated
- Every paragraph translated
- Every code block preserved exactly
- File length ~180-220 lines (same as README.md)
- Natural Spanish (not machine-translation artifacts)
  </action>
  
  <acceptance_criteria>
    - README.es.md exists at project root
    - Contains "## Instalación" section (not "## Installation")
    - Contains "## Uso de CLI" section
    - Contains "## Solución de Problemas" with 3 subsections
    - All 4 CLI bash examples are byte-for-byte identical to English version
    - All curl examples are byte-for-byte identical to English version
    - Flag names remain in English (--input, not --entrada)
    - File is ~180-220 lines (wc -l)
    - Spanish prose is natural and clear (not machine-translated gibberish)
  </acceptance_criteria>
  
  <done>
README.es.md exists at project root as complete Spanish translation of README.md. All prose translated, all code preserved, same structure. Spanish-speaking developers can follow it independently.
  </done>
</task>

## Verification

After completion:
- [ ] README.es.md exists and is ~200 lines
- [ ] Headings are in Spanish
- [ ] Code blocks are identical to README.md
- [ ] Spanish prose is natural and grammatically correct
- [ ] File structure mirrors README.md exactly

## Success Criteria

Spanish-speaking developer following README.es.md can install and use GenSubtitles without consulting English README. Translation is complete and accurate.

## Output

After completion, create `.planning/phases/10-documentation-end-to-end-validation/10-02-SUMMARY.md`
