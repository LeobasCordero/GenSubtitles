---
phase: 999.32-gui-mejoras-ux-paletas-de-colores-y-accesibilidad
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - gensubtitles/gui/theme.py
  - gensubtitles/gui/styles.py
  - gensubtitles/core/settings.py
  - gensubtitles/gui/locale.py
autonomous: true
requirements:
  - D-05-secondary-btn-contrast
  - D-06-palette-foundation
  - D-04-clear-console-setting
  - D-07-log-i18n-strings
  - D-03-log-separator-key

must_haves:
  truths:
    - "theme.p() resolves user overrides before falling back to _PALETTES"
    - "All 6 named palettes exist in _PALETTES (Dark, Light, Ocean Dark, Emerald, Sunset, Monochrome) and each has 17 tokens including btn_secondary_text"
    - "apply_secondary_btn_style() sets text_color to p('btn_secondary_text')"
    - "AppSettings dataclass has clear_console_on_clear, active_palette, and palette_overrides fields"
    - "locale.py _STRINGS has all new log, separator, palette-UI, and clear-console keys in both en and es"
  artifacts:
    - path: "gensubtitles/gui/theme.py"
      provides: "set_active_palette(), _active_palette_tokens, _user_overrides, 6-palette _PALETTES, modified p()"
      contains: "def set_active_palette"
    - path: "gensubtitles/gui/styles.py"
      provides: "apply_secondary_btn_style with text_color"
      contains: "btn_secondary_text"
    - path: "gensubtitles/core/settings.py"
      provides: "AppSettings with 3 new fields"
      contains: "clear_console_on_clear"
    - path: "gensubtitles/gui/locale.py"
      provides: "~50 new locale keys in en and es"
      contains: "log_separator"
  key_links:
    - from: "gensubtitles/gui/main.py"
      to: "gensubtitles/gui/theme.py"
      via: "theme.set_active_palette() called from _apply_startup_settings()"
      pattern: "set_active_palette"
    - from: "gensubtitles/gui/styles.py"
      to: "gensubtitles/gui/theme.py"
      via: "p('btn_secondary_text')"
      pattern: "btn_secondary_text"
---

<objective>
Build the foundation layer for Phase 999.32: add palette infrastructure to theme.py, fix secondary button text contrast in styles.py, add 3 new AppSettings fields, and add all new locale keys.

Purpose: All subsequent plans (02, 03) depend on these tokens, settings fields, and locale keys existing. This plan must complete before any main.py changes.
Output: theme.py, styles.py, core/settings.py, locale.py all updated.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.32-gui-mejoras-ux-paletas-de-colores-y-accesibilidad/999.32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: theme.py — Palette infrastructure + 4 new palettes + D-05 token</name>

  <read_first>
    - gensubtitles/gui/theme.py (full file — understand existing _PALETTES structure and p() implementation)
  </read_first>

  <action>
**Step 1: Add `btn_secondary_text` to existing `_PALETTES` (D-05)**

In `_PALETTES["Dark"]`, add after `"secondary_hov"`:
```python
"btn_secondary_text": "#E0E0E0",
```

In `_PALETTES["Light"]`, add after `"secondary_hov"`:
```python
"btn_secondary_text": "#212121",
```

**Step 2: Add 4 new palettes to `_PALETTES`** (insert before the closing `}` of the dict):

```python
    "Ocean Dark": {
        "bg":                 "#0A1628",
        "surface":            "#112240",
        "input_bg":           "#1A3050",
        "text_primary":       "#CCD6F6",
        "text_secondary":     "#8892B0",
        "accent":             "#64FFDA",
        "accent_hov":         "#52E0BE",
        "secondary":          "#233554",
        "secondary_hov":      "#2D4470",
        "btn_secondary_text": "#CCD6F6",
        "progress_idle":      "#4A5568",
        "progress_proc":      "#64FFDA",
        "progress_done":      "#52E0BE",
        "progress_err":       "#FF6B6B",
        "menu_bg":            "#061122",
        "menu_fg":            "#CCD6F6",
        "menu_active_bg":     "#1A3050",
    },
    "Emerald": {
        "bg":                 "#0D1F12",
        "surface":            "#1A3020",
        "input_bg":           "#243D28",
        "text_primary":       "#E2F4E8",
        "text_secondary":     "#A8C9B0",
        "accent":             "#2ECC71",
        "accent_hov":         "#27AE60",
        "secondary":          "#2D4A35",
        "secondary_hov":      "#3A5E44",
        "btn_secondary_text": "#E2F4E8",
        "progress_idle":      "#4A7055",
        "progress_proc":      "#2ECC71",
        "progress_done":      "#27AE60",
        "progress_err":       "#E74C3C",
        "menu_bg":            "#080F0A",
        "menu_fg":            "#E2F4E8",
        "menu_active_bg":     "#1A3020",
    },
    "Sunset": {
        "bg":                 "#FFF8F0",
        "surface":            "#FFFFFF",
        "input_bg":           "#FDE8D8",
        "text_primary":       "#2D1810",
        "text_secondary":     "#7A4030",
        "accent":             "#E85D04",
        "accent_hov":         "#C44E00",
        "secondary":          "#F3C5A5",
        "secondary_hov":      "#E8A880",
        "btn_secondary_text": "#2D1810",
        "progress_idle":      "#C4A080",
        "progress_proc":      "#E85D04",
        "progress_done":      "#388E3C",
        "progress_err":       "#C62828",
        "menu_bg":            "#FFFFFF",
        "menu_fg":            "#2D1810",
        "menu_active_bg":     "#FDE8D8",
    },
    "Monochrome": {
        "bg":                 "#0C0C0C",
        "surface":            "#1A1A1A",
        "input_bg":           "#252525",
        "text_primary":       "#F0F0F0",
        "text_secondary":     "#A0A0A0",
        "accent":             "#FFFFFF",
        "accent_hov":         "#E0E0E0",
        "secondary":          "#333333",
        "secondary_hov":      "#444444",
        "btn_secondary_text": "#F0F0F0",
        "progress_idle":      "#666666",
        "progress_proc":      "#FFFFFF",
        "progress_done":      "#CCCCCC",
        "progress_err":       "#FF4444",
        "menu_bg":            "#080808",
        "menu_fg":            "#F0F0F0",
        "menu_active_bg":     "#252525",
    },
```

**Step 3: Add module-level state and new functions** (insert after the `_PALETTES` dict, before `def p()`):

```python
# ---------------------------------------------------------------------------
# Runtime palette override state (Phase 999.32)
# ---------------------------------------------------------------------------
# Mutable module-level state so p() resolves the active palette at call time.
# set_active_palette() is called from GenSubtitlesApp._apply_startup_settings()
# and from the palette panel Save handler.

_active_palette_tokens: dict[str, str] = {}  # tokens for non-default palette
_user_overrides: dict[str, str] = {}         # user's per-token customizations

#: Names of palettes that use mode-based fallback (no tokens override needed).
_DEFAULT_PALETTE_NAMES: frozenset[str] = frozenset({"Default Dark", "Default Light"})


def set_active_palette(
    palette_name: str,
    user_overrides: "dict[str, str] | None" = None,
) -> None:
    """Set the active named palette and optional per-token user overrides.

    Call this after loading settings (in _apply_startup_settings) and after
    the user saves the palette panel.

    Args:
        palette_name: A key in _PALETTES, or "Default Dark" / "Default Light"
                      to use the mode-based fallback.
        user_overrides: Dict of {token_key: hex_color} for user customizations.
                        Pass None or {} to clear any previous overrides.
    """
    _active_palette_tokens.clear()
    _user_overrides.clear()
    if palette_name not in _DEFAULT_PALETTE_NAMES:
        tokens = _PALETTES.get(palette_name, {})
        _active_palette_tokens.update(tokens)
    if user_overrides:
        _user_overrides.update(user_overrides)


#: Constant listing all palette names in display order.
PALETTE_NAMES: tuple[str, ...] = (
    "Default Dark", "Default Light",
    "Ocean Dark", "Emerald", "Sunset", "Monochrome",
)
```

**Step 4: Modify `p()` to check overrides first**:

Replace the existing `p()` function with:
```python
def p(key: str) -> str:
    """Return the colour token for the current effective appearance mode.

    Resolution order:
    1. Per-token user overrides (_user_overrides) — highest priority.
    2. Active named palette tokens (_active_palette_tokens) — non-default palette.
    3. Mode-based _PALETTES fallback — Default Dark / Default Light.
    """
    if key in _user_overrides:
        return _user_overrides[key]
    if _active_palette_tokens:
        return _active_palette_tokens.get(key, "#FF0000")
    mode = ctk.get_appearance_mode()  # resolves "System" → "Dark" or "Light"
    return _PALETTES.get(mode, _PALETTES["Dark"]).get(key, "#FF0000")
```
  </action>

  <verify>
    <automated>python -c "from gensubtitles.gui.theme import p, set_active_palette, PALETTE_NAMES, _PALETTES; set_active_palette('Ocean Dark'); assert p('accent') == '#64FFDA'; set_active_palette('Default Dark'); print('theme OK')"</automated>
  </verify>

  <acceptance_criteria>
    - grep "btn_secondary_text" gensubtitles/gui/theme.py returns matches in Dark, Light, Ocean Dark, Emerald, Sunset, and Monochrome palette blocks
    - grep "def set_active_palette" gensubtitles/gui/theme.py returns a match
    - grep "PALETTE_NAMES" gensubtitles/gui/theme.py returns a match containing "Ocean Dark"
    - grep "_user_overrides" gensubtitles/gui/theme.py shows both declaration and use inside p()
    - python -c "from gensubtitles.gui.theme import _PALETTES; assert len(_PALETTES) == 6" exits 0
    - python -c "from gensubtitles.gui.theme import _PALETTES; assert 'btn_secondary_text' in _PALETTES['Dark']" exits 0
  </acceptance_criteria>

  <done>theme.py has 6 palettes each with 17 tokens, set_active_palette() exported, p() checks overrides first, import succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: styles.py — Secondary button text_color + settings.py — 3 new AppSettings fields + locale.py — all new keys</name>

  <read_first>
    - gensubtitles/gui/styles.py (full file — understand apply_secondary_btn_style location)
    - gensubtitles/core/settings.py (full file — understand AppSettings dataclass structure and field ordering)
    - gensubtitles/gui/locale.py (full file — understand _STRINGS structure, both "en" and "es" dicts)
  </read_first>

  <action>
### A. styles.py — Fix secondary button text contrast (D-05)

In `apply_secondary_btn_style()`, add `text_color`:
```python
def apply_secondary_btn_style(widget: object) -> None:
    """Apply secondary colours to *widget* (CTkButton)."""
    widget.configure(  # type: ignore[attr-defined]
        fg_color=p("secondary"),
        hover_color=p("secondary_hov"),
        text_color=p("btn_secondary_text"),
    )
```

### B. core/settings.py — 3 new AppSettings fields (D-04, D-06)

In the `AppSettings` dataclass, add after `subtitle_outline_color`:

```python
# Phase 999.32 — clear console + palette system
clear_console_on_clear: bool = False   # D-04: clear active tab console on "Limpiar campos"
active_palette: str = "Default Dark"   # D-06: selected palette name (key in theme._PALETTES or "Default Dark"/"Default Light")
palette_overrides: dict = field(default_factory=dict)  # D-06: {token: hex} user customizations
```

Add `field` to the imports: `from dataclasses import asdict, dataclass, field`

### C. locale.py — All new string keys (D-03, D-04, D-06, D-07)

Add the following keys to `_STRINGS["en"]` (after the existing Phase 999.30 section):

```python
        # Phase 999.32 — separator, clear console, palette UI
        "log_separator":                "══════════════════════════════════════════════════",
        "clear_console_on_clear_lbl":   "Clear console on Clear fields:",
        "menu_color_palette":           "Color Palette\u2026",
        "palette_header":               "Color Palette",
        "palette_active_lbl":           "Active palette:",
        "palette_reset_btn":            "Reset to defaults",
        "palette_save_btn":             "Save",
        "palette_back_btn":             "Back",
        # Palette token labels
        "token_bg":                     "Background",
        "token_surface":                "Surface / Card",
        "token_input_bg":               "Input / Console bg",
        "token_text_primary":           "Primary text",
        "token_text_secondary":         "Secondary text",
        "token_accent":                 "Primary button",
        "token_accent_hov":             "Primary button hover",
        "token_secondary":              "Secondary button",
        "token_secondary_hov":          "Secondary button hover",
        "token_btn_secondary_text":     "Secondary button text",
        "token_progress_idle":          "Progress idle",
        "token_progress_proc":          "Progress active",
        "token_progress_done":          "Progress done",
        "token_progress_err":           "Progress error",
        "token_menu_bg":                "Menu background",
        "token_menu_fg":                "Menu text",
        "token_menu_active_bg":         "Menu active background",
        # Log messages — warnings (D-07)
        "log_warn_no_input_video":      "\u26a0\ufe0f Please select an input video file.",
        "log_warn_no_input_audio":      "\u26a0\ufe0f Please select an input audio file.",
        "log_warn_no_input_subtitle":   "\u26a0\ufe0f Please select a subtitle file.",
        "log_warn_not_wav":             "\u26a0\ufe0f Transcribe step requires a .wav file.",
        "log_warn_output_not_wav":      "\u26a0\ufe0f Output audio file must use .wav extension.",
        "log_warn_no_output_dir":       "\u26a0\ufe0f Cannot determine output directory.",
        "log_warn_no_target_lang":      "\u26a0\ufe0f Please select a target language.",
        "log_warn_no_input_transcription": "\u26a0\ufe0f Please select a transcription file.",
        "log_warn_no_input_file":       "\u26a0\ufe0f Please select an input file.",
        "log_warn_file_not_found":      "\u26a0\ufe0f Selected file was not found.",
        # Log messages — progress / success (D-07)
        "log_step_success_extract":     "\u2713 Audio extracted successfully.",
        "log_step_success_transcribe":  "\u2713 Transcription complete.",
        "log_step_success_translate":   "\u2713 Translation complete.",
        "log_step_success_write":       "\u2713 Subtitle written:",
        "log_prefill_tab4":             "\u2192 Pre-filled Tab 4 input:",
        "log_prefill_tab5":             "\u2192 Pre-filled Tab 5 input:",
        "log_prefill_tab6":             "\u2192 Pre-filled Tab 6 input:",
        # Log messages — errors (D-07)
        "log_error_extract":            "\u2717 Extract failed:",
        "log_error_transcribe":         "\u2717 Transcription failed:",
        "log_error_translate":          "\u2717 Translation failed:",
        "log_error_write":              "\u2717 Write failed:",
        "log_error_cannot_apply_output_name": "\u26a0\ufe0f Could not apply custom output name:",
```

Add the same keys to `_STRINGS["es"]` (after the existing Phase 999.30 section):

```python
        # Phase 999.32 — separator, consola limpia, paleta
        "log_separator":                "══════════════════════════════════════════════════",
        "clear_console_on_clear_lbl":   "Limpiar consola al limpiar campos:",
        "menu_color_palette":           "Paleta de colores\u2026",
        "palette_header":               "Paleta de colores",
        "palette_active_lbl":           "Paleta activa:",
        "palette_reset_btn":            "Restablecer",
        "palette_save_btn":             "Guardar",
        "palette_back_btn":             "Volver",
        # Etiquetas de tokens
        "token_bg":                     "Fondo",
        "token_surface":                "Superficie / Tarjeta",
        "token_input_bg":               "Fondo de entrada / consola",
        "token_text_primary":           "Texto principal",
        "token_text_secondary":         "Texto secundario",
        "token_accent":                 "Bot\u00f3n primario",
        "token_accent_hov":             "Bot\u00f3n primario (hover)",
        "token_secondary":              "Bot\u00f3n secundario",
        "token_secondary_hov":          "Bot\u00f3n secundario (hover)",
        "token_btn_secondary_text":     "Texto bot\u00f3n secundario",
        "token_progress_idle":          "Progreso inactivo",
        "token_progress_proc":          "Progreso activo",
        "token_progress_done":          "Progreso completado",
        "token_progress_err":           "Progreso error",
        "token_menu_bg":                "Fondo men\u00fa",
        "token_menu_fg":                "Texto men\u00fa",
        "token_menu_active_bg":         "Fondo men\u00fa activo",
        # Mensajes de log — advertencias (D-07)
        "log_warn_no_input_video":      "\u26a0\ufe0f Por favor selecciona un archivo de video de entrada.",
        "log_warn_no_input_audio":      "\u26a0\ufe0f Por favor selecciona un archivo de audio de entrada.",
        "log_warn_no_input_subtitle":   "\u26a0\ufe0f Por favor selecciona un archivo de subt\u00edtulos.",
        "log_warn_not_wav":             "\u26a0\ufe0f El paso de transcripci\u00f3n requiere un archivo .wav.",
        "log_warn_output_not_wav":      "\u26a0\ufe0f El archivo de audio de salida debe usar extensi\u00f3n .wav.",
        "log_warn_no_output_dir":       "\u26a0\ufe0f No se puede determinar el directorio de salida.",
        "log_warn_no_target_lang":      "\u26a0\ufe0f Por favor selecciona un idioma de destino.",
        "log_warn_no_input_transcription": "\u26a0\ufe0f Por favor selecciona un archivo de transcripci\u00f3n.",
        "log_warn_no_input_file":       "\u26a0\ufe0f Por favor selecciona un archivo de entrada.",
        "log_warn_file_not_found":      "\u26a0\ufe0f El archivo seleccionado no fue encontrado.",
        # Mensajes de log — progreso / \u00e9xito (D-07)
        "log_step_success_extract":     "\u2713 Audio extra\u00eddo correctamente.",
        "log_step_success_transcribe":  "\u2713 Transcripci\u00f3n completada.",
        "log_step_success_translate":   "\u2713 Traducci\u00f3n completada.",
        "log_step_success_write":       "\u2713 Subt\u00edtulo escrito:",
        "log_prefill_tab4":             "\u2192 Pre-llenado Tab 4 entrada:",
        "log_prefill_tab5":             "\u2192 Pre-llenado Tab 5 entrada:",
        "log_prefill_tab6":             "\u2192 Pre-llenado Tab 6 entrada:",
        # Mensajes de log — errores (D-07)
        "log_error_extract":            "\u2717 Extracci\u00f3n fallida:",
        "log_error_transcribe":         "\u2717 Transcripci\u00f3n fallida:",
        "log_error_translate":          "\u2717 Traducci\u00f3n fallida:",
        "log_error_write":              "\u2717 Escritura fallida:",
        "log_error_cannot_apply_output_name": "\u26a0\ufe0f No se pudo aplicar el nombre de salida personalizado:",
```
  </action>

  <verify>
    <automated>python -c "from gensubtitles.gui.styles import apply_secondary_btn_style; from gensubtitles.core.settings import AppSettings; from dataclasses import fields; s = AppSettings(); assert s.clear_console_on_clear == False; assert s.active_palette == 'Default Dark'; assert s.palette_overrides == {}; from gensubtitles.gui.locale import s as ls, set_language; set_language('en'); assert ls('log_separator').startswith('\u2550'); assert ls('log_warn_no_input_video').startswith('\u26a0'); set_language('es'); assert ls('log_warn_no_input_video').startswith('\u26a0'); print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - grep "btn_secondary_text" gensubtitles/gui/styles.py returns 1 match in apply_secondary_btn_style
    - grep "clear_console_on_clear" gensubtitles/core/settings.py returns 1 match in AppSettings body
    - grep "active_palette" gensubtitles/core/settings.py returns 1 match
    - grep "palette_overrides" gensubtitles/core/settings.py returns 1 match
    - grep "from dataclasses import" gensubtitles/core/settings.py contains "field"
    - grep "log_separator" gensubtitles/gui/locale.py returns 2 matches (one in "en", one in "es")
    - grep "log_warn_no_input_video" gensubtitles/gui/locale.py returns 2 matches
    - grep "menu_color_palette" gensubtitles/gui/locale.py returns 2 matches
    - grep "token_bg" gensubtitles/gui/locale.py returns 2 matches
    - python -c "from gensubtitles.core.settings import AppSettings; AppSettings()" exits 0
    - python -c "from gensubtitles.gui.locale import _STRINGS; assert 'log_separator' in _STRINGS['en'] and 'log_separator' in _STRINGS['es']" exits 0
  </acceptance_criteria>

  <done>styles.py apply_secondary_btn_style sets text_color. AppSettings has 3 new fields with defaults. locale.py has ~50 new keys in both en and es. All imports succeed.</done>
</task>

</tasks>

<verification>
Run after both tasks:

```
python -c "
from gensubtitles.gui import theme, styles
from gensubtitles.core.settings import AppSettings
from gensubtitles.gui.locale import s, set_language, _STRINGS

# Theme
theme.set_active_palette('Emerald')
assert theme.p('accent') == '#2ECC71'
theme.set_active_palette('Default Dark')

# All 6 palettes present
assert len(theme._PALETTES) == 6
assert 'btn_secondary_text' in theme._PALETTES['Dark']

# AppSettings new fields
s2 = AppSettings()
assert s2.clear_console_on_clear == False
assert s2.active_palette == 'Default Dark'
assert s2.palette_overrides == {}

# Locale coverage
set_language('en')
assert s('log_separator').startswith('\u2550')
assert s('menu_color_palette').endswith('\u2026')
set_language('es')
assert s('log_warn_no_input_video').startswith('\u26a0')
assert s('palette_header') == 'Paleta de colores'
assert 'token_bg' in _STRINGS['es']

print('Plan 01 verification PASSED')
"
```
</verification>

<success_criteria>
- theme.py: 6 palettes × 17 tokens, set_active_palette() exported, PALETTE_NAMES tuple, p() with override priority
- styles.py: apply_secondary_btn_style sets text_color=p("btn_secondary_text")
- settings.py: AppSettings has clear_console_on_clear, active_palette, palette_overrides; dataclasses.field imported
- locale.py: ~50 new keys in both en and es; all start with correct emoji/character conventions
</success_criteria>

<output>
After completion, create `.planning/phases/999.32-gui-mejoras-ux-paletas-de-colores-y-accesibilidad/999.32-01-SUMMARY.md`
</output>
