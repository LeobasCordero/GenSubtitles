---
phase: 999.32-gui-mejoras-ux-paletas-de-colores-y-accesibilidad
plan: "02"
subsystem: gui
tags: [customtkinter, ux, button-locking, auto-fill, i18n, locale]

requires:
  - phase: 999.32-01
    provides: "locale keys (log_separator, log_warn_*, log_step_success_*, log_error_*, log_prefill_*)"
provides:
  - "_on_browse_extract_input() auto-fills 5 downstream StringVars when video is selected in Tab 3"
  - "_set_all_action_buttons() disables/enables 20+ buttons across all tabs during step execution"
  - "_log_separator() prints ══…══ visual separator before each step run"
  - "All 4 tab clear buttons stored as self._*_btn_clear attributes"
  - "All hardcoded warning/success/error strings in tabs 3-6 replaced with s() locale calls"
affects: [999.32-03]

tech-stack:
  added: []
  patterns:
    - "Button locking via _set_all_action_buttons() called at _run_step_in_bg() entry and in finally block"
    - "Auto-fill downstream fields from video path stem in _on_browse_extract_input()"
    - "Locale string calls: s('log_warn_*'), s('log_step_success_*'), s('log_error_*')"

key-files:
  created: []
  modified:
    - gensubtitles/gui/main.py

key-decisions:
  - "_btn_generate and _tl_btn_translate excluded from re-enable in _set_all_action_buttons() — managed by server-ready handlers"
  - "Auto-fill uses stem-derived names: .wav for extract/transcribe, .json for translate, _translated.json for write input, .srt for write output"
  - "finally block in _run_step_in_bg worker ensures buttons re-enable even on exception"

patterns-established:
  - "_set_all_action_buttons('disabled') / 'normal' pattern for step execution lifecycle"

requirements-completed:
  - D-01-filename-chain
  - D-02-button-locking
  - D-03-log-separator
  - D-07-log-i18n-main

duration: 20min
completed: 2026-04-29
---

# Phase 999.32 Plan 02: Tab UX Features Summary

**Auto-fill filename chain (D-01), button locking during step execution (D-02), console separator (D-03), and locale-ized log strings in tabs 3-6 (D-07) — all implemented in main.py.**

## Performance

- **Duration:** 20 min
- **Started:** 2026-04-29T00:15:00Z
- **Completed:** 2026-04-29T00:35:00Z
- **Tasks:** 2 completed
- **Files modified:** 1

## Accomplishments

### Task 1: Clear buttons + auto-fill browse
- Converted `_extract_btn_clear`, `_transcribe_btn_clear`, `_translate_step_btn_clear`, `_write_btn_clear` from local variables to `self` attributes
- Replaced Tab 3 browse lambda with dedicated `_on_browse_extract_input()` method
- `_on_browse_extract_input()` sets video input and auto-fills: extract output (.wav), transcribe input (.wav), translate step input (.json), write input (_translated.json), write output (.srt) — all stem-derived

### Task 2: Button locking + separator + locale
- Added `_log_separator()` helper (calls `_log_to` with `s("log_separator")`)
- Added `_set_all_action_buttons(state)` — controls 20 buttons across tabs 3-6 (Tab 1/2 generate/translate excluded from re-enable since managed by server-ready handlers)
- Wired into `_run_step_in_bg()`: separator + disable at start, `finally` block re-enables on completion or error
- Replaced all 24 hardcoded `⚠️`, `✓`, `✗`, `→` strings in `_on_tab3_extract` through `_on_tab6_write` with `s()` calls

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness

Ready for 999.32-03: clear-console toggle and Color Palette settings panel.

## Self-Check: PASSED
