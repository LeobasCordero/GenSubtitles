# Phase 999.9: GUI — Form Polish - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-10
**Phase:** 999.9 — GUI Form Polish (mandatory field markers, hide source language, target language dropdown)

---

## Area 1: Mandatory Field Markers

**Q:** How/where does the asterisk appear? Label text, separate colored label, or styled inline?

| Option | Description |
|--------|-------------|
| A | Append `*` to label text (`"Input video *:"`) |
| B | Separate red `CTkLabel` widget per field |
| C | Agent's discretion |

**Selected:** A — Append to label text

---

## Area 2: Source Language — Hiding

**Q:** How is the source language field hidden?

| Option | Description |
|--------|-------------|
| A | `.grid_remove()` on both label and entry (widget stays in memory) |
| B | Agent's discretion |

**Selected:** A — `.grid_remove()` on both label and `_entry_source_lang` after `.grid()` in `_build_ui`

---

## Area 3: Target Language Dropdown — Widget and Values

**Q1:** What widget type?
- Discussed: `CTkOptionMenu` (selection-only) vs `CTkComboBox` (editable)
- **Selected:** `CTkOptionMenu` — aligns with the phase goal of reducing user error

**Q2:** What values does the dropdown show?

| Option | Description |
|--------|-------------|
| A | Friendly names (`"Spanish"`, `"English"`, `"Other"`) mapped to codes |
| B | Language codes directly (`"es"`, `"en"`, `"other"`) |
| C | Agent's discretion |

**Selected:** A — friendly names mapped to API codes via `_LANG_MAP`

**Q3:** Default selection on launch?

| Option | Description |
|--------|-------------|
| A | `"(none — no translation)"` — matches current placeholder behavior |
| B | `"Spanish"` — pre-selects most common use case |
| C | Agent's discretion |

**Selected:** B — `"Spanish"` pre-selected on launch

---

## Area 4: "Other" Option Behavior

**Q:** When user selects "Other" from the dropdown, what happens?

| Option | Description |
|--------|-------------|
| A | Reveal a free-text `CTkEntry` below the dropdown for typing a language code |
| B | Agent's discretion |

**Selected:** A — "Other" reveals a free-text `CTkEntry` below the dropdown; value passed as `target_lang` to API

---

*Discussion log generated: 2026-04-10*
