---
phase: "999.9"
plan: "01"
subsystem: ui
tags: [customtkinter, gui, form, ux]

requires: []
provides:
  - Mandatory field markers (* suffix) on Input video and Output SRT labels
  - Source language row hidden via grid_remove (Whisper auto-detects)
  - Target language CTkOptionMenu dropdown (Spanish/English/Other)
  - Conditional Other free-text entry that reveals/hides on selection change
  - _LANG_MAP constant mapping display names to ISO codes (es, en)
  - _on_target_lang_change method driving show/hide of Other entry
affects: [gui]

tech-stack:
  added: []
  patterns:
    - CTkOptionMenu with variable= (not textvariable=) + command= callback for reactive dropdowns
    - grid_remove() for hiding rows while preserving grid metadata
    - Module-level _LANG_MAP dict for display→code translation

key-files:
  created: []
  modified:
    - gensubtitles/gui/main.py

key-decisions:
  - "Used grid_remove() on both label and entry for source-language row so widgets can be grid()-restored later if needed"
  - "CTkOptionMenu uses variable= StringVar initialized to 'Spanish' so default is set at construction"
  - "Other entry hidden at rest (row=4), revealed only when _on_target_lang_change receives 'Other'"
  - "_update_clear_state checks only _input_var, _output_var, _target_lang_other_var (dropdown always has a value)"
  - "_on_clear resets dropdown to 'Spanish' and hides Other entry explicitly"

patterns-established:
  - "Conditional widget visibility: grid_remove() + grid() inside a command callback"
  - "Lang code mapping: module-level dict + dict.get() fallback to free-text var"

requirements-completed: []

duration: 20min
completed: 2026-04-10
---

# Phase 999.9: GUI Form Polish Summary

**Mandatory field markers, hidden source-language row, and a Spanish/English/Other dropdown replace the flat target-language text entry — three bundled UX improvements committed on `feature/999.9-gui-form-polish`, human-verified at runtime.**
