---
plan: 999.11-01
phase: 999.11-subtitle-silence-vad-quality
status: complete
committed: ed9a62b
---

## Summary

Implemented VAD parameter tuning, word-level timestamps, end-time tightening, and wordless segment dropping in `transcriber.py`. Changed default model size from `small` to `medium` in both `WhisperTranscriber.__init__` and `transcribe_audio()`. Added 7 new TDD tests covering all new behaviors; updated `_make_fake_segment` helper to include a `words` attribute so pre-existing tests remain compatible with the new patching loop.

## Key Files

### key-files.created
- `tests/test_transcriber.py` — 7 new tests added (D-01 through D-06 behaviors)

### key-files.modified
- `gensubtitles/core/transcriber.py` — VAD params, word timestamps, end-time patching loop, medium defaults

## Decisions

- Updated `_make_fake_segment()` helper in the test file to include `words=[SingleWordStub]` with `word.end == seg.end` so existing tests are unaffected by the new drop/tighten loop.
- Used `getattr(seg, "words", None)` in patching loop for compatibility with both namedtuple and SimpleNamespace segments.
- Used `seg._replace(end=...)` (namedtuple path) with `copy.copy` fallback (SimpleNamespace path) for end-time mutation.

## Test Results

- `tests/test_transcriber.py`: 34/34 passed (27 original + 7 new)
- Full suite (excluding e2e): 144/145 passed (1 pre-existing unrelated Windows file-lock failure in `test_translator.py`)

## Self-Check: PASSED
