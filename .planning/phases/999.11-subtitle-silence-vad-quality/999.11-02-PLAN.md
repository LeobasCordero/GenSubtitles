---
phase: 999.11-subtitle-silence-vad-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.es.md
  - gensubtitles/gui/main.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "README.md --model flag table shows 'medium' as default (not 'small')"
    - "README.md warns about ~1.5 GB first-run Whisper model download"
    - "README.es.md mirrors all README.md model-size changes in Spanish"
    - "GUI tutorial text reflects medium as the new default model"
  artifacts:
    - path: "README.md"
      provides: "Updated --model default + download size warning"
      contains: "medium"
    - path: "README.es.md"
      provides: "Spanish translation of model-size updates"
      contains: "medium"
    - path: "gensubtitles/gui/main.py"
      provides: "Updated tutorial text mentioning medium default"
      contains: "medium"
  key_links:
    - from: "README.md line ~75"
      to: "CLI flag table default column"
      via: "table cell update"
      pattern: "\\| `small` \\|"
---

<objective>
Update all user-facing documentation to reflect that the default Whisper model size is now `medium` (not `small`), and warn users about the ~1.5 GB first-run download.

Purpose: Prevent user confusion when the app downloads a larger model than expected on first run.
Output: Updated README.md, README.es.md, and GUI tutorial text.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.11-subtitle-silence-vad-quality/999.11-CONTEXT.md
@README.md
@README.es.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md and README.es.md CLI flag table and model download note</name>
  <files>README.md, README.es.md</files>
  <action>
    **README.md — two changes:**

    1. Update the CLI flag table (line ~75) — change the default column for `--model` from `small` to `medium`:
       Find: `| `--model`, `-m` | Whisper model size: ... | `small` |`
       Replace the trailing `| \`small\` |` with `| \`medium\` |`

    2. Add a Whisper model download size note near the "First-time model download" heading at line ~136.
       After the existing line "On first use of a language pair (e.g., English → Spanish)..."
       insert a new bullet point:
       ```
       - On first run, the default Whisper transcription model (`medium`, ~1.5 GB) is downloaded automatically; use `--model small` or `--model tiny` to download a lighter model instead
       ```
       This should appear BEFORE the existing "Models are downloaded from the internet" line,
       or as a new Note block after the translation models paragraph — whichever reads more naturally.
       
       Exact recommended placement: add a new subsection or note directly below the `--model` flag table
       under the existing CLI Flags section:
       ```markdown
       > **First run:** The default model (`medium`) requires a one-time ~1.5 GB download.
       > Use `--model small` (~470 MB) or `--model tiny` (~75 MB) for a smaller initial download.
       ```

    **README.es.md — mirror all changes in Spanish:**

    1. Update the `--model` default in the equivalent flag table from `small` → `medium`.

    2. Add the Spanish equivalent of the first-run download note:
       ```markdown
       > **Primera ejecución:** El modelo por defecto (`medium`) requiere una descarga única de ~1,5 GB.
       > Usa `--model small` (~470 MB) o `--model tiny` (~75 MB) para una descarga inicial más pequeña.
       ```
  </action>
  <verify>
    <automated>grep -n "medium" README.md README.es.md</automated>
  </verify>
  <done>
    `grep "medium" README.md` shows at least 2 matches: one in the flag table default column and one in the download note.
    `grep "medium" README.es.md` shows the same pattern in Spanish.
    `grep "small" README.md` no longer shows `| \`small\` |` as the default in the flag table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update GUI tutorial text to mention medium default</name>
  <files>gensubtitles/gui/main.py</files>
  <action>
    In `gensubtitles/gui/main.py`, find the TROUBLESHOOTING section of the tutorial text
    (inside `_show_tutorial`, the `tutorial_text` string, line ~951):

    Current line:
    ```
    • Subtitles are blank — Try a smaller model (tiny or base) or check if audio track exists.
    ```

    Replace with:
    ```
    • Subtitles are blank — The default model is `medium` (~1.5 GB first-run download). Try `--model small` or `--model tiny` for a lighter option, or check if the video has an audio track.
    ```

    This change is in the multiline string inside `_show_tutorial()` in `gensubtitles/gui/main.py`.
    Do NOT change any other part of the file.
  </action>
  <verify>
    <automated>grep -n "medium" gensubtitles/gui/main.py</automated>
  </verify>
  <done>
    `grep "medium" gensubtitles/gui/main.py` shows at least 1 match in the tutorial text.
    `python -c "from gensubtitles.gui import main"` imports without error (syntax check).
  </done>
</task>

</tasks>

<verification>
Quick search confirms all three files updated:
`grep -c "medium" README.md README.es.md gensubtitles/gui/main.py`
Each should show count >= 1.

Import check (no GUI window opened, just syntax):
`python -c "import ast; ast.parse(open('gensubtitles/gui/main.py').read()); print('GUI syntax OK')`
</verification>

<success_criteria>
- README.md CLI flag table shows `medium` as the `--model` default
- README.md has a first-run download size note (~1.5 GB)
- README.es.md mirrors both changes in Spanish
- GUI tutorial troubleshooting text mentions `medium` as the default and provides lighter alternatives
- No other files modified
</success_criteria>

<output>
After completion, create `.planning/phases/999.11-subtitle-silence-vad-quality/999.11-02-SUMMARY.md`
</output>
