---
phase: 999.11-subtitle-silence-vad-quality
status: passed
verified: 2026-04-11
---

## Verification Results

All must-haves confirmed against codebase.

### Truths Verified

| Truth | Status | Evidence |
|-------|--------|----------|
| VAD uses tuned parameters: min_silence_duration_ms=400, speech_pad_ms=200, min_speech_duration_ms=250 | ✓ PASS | `transcriber.py` lines 139-143: `kwargs["vad_parameters"] = {...}` |
| word_timestamps=True is passed to every model.transcribe() call | ✓ PASS | `transcriber.py` line 145: `kwargs["word_timestamps"] = True` |
| Each segment's .end is replaced with its last word's .end | ✓ PASS | `transcriber.py` patching loop; test `test_segment_end_tightened_to_last_word_end` passes |
| Segments with None or empty .words list are dropped | ✓ PASS | `transcriber.py` patching loop `if not words: continue`; tests D/E pass |
| Default model_size is 'medium' in WhisperTranscriber.__init__() and transcribe_audio() | ✓ PASS | `transcriber.py` lines 58, 213; tests F/G pass |
| README.md --model flag table shows 'medium' as default | ✓ PASS | README.md line 75 |
| README.md warns about ~1.5 GB first-run download | ✓ PASS | README.md line 80 (blockquote) |
| README.es.md mirrors all changes in Spanish | ✓ PASS | README.es.md lines 75, 80 |
| GUI tutorial text reflects medium as the new default model | ✓ PASS | `gui/main.py` line 951 |

### Artifacts Verified

| Artifact | Contains | Status |
|----------|---------|--------|
| `gensubtitles/core/transcriber.py` | `vad_parameters`, `word_timestamps=True`, patching loop, `"medium"` defaults | ✓ EXISTS |
| `tests/test_transcriber.py` | 34 tests (7 new), all pass | ✓ EXISTS |
| `README.md` | `medium` default + download note | ✓ EXISTS |
| `README.es.md` | Spanish mirror of model-size updates | ✓ EXISTS |
| `gensubtitles/gui/main.py` | `medium` in tutorial troubleshooting | ✓ EXISTS |

### Key Links Verified

| From | To | Via | Status |
|------|----|-----|--------|
| `transcribe()` kwargs dict | `model.transcribe()` | `vad_parameters` dict + `word_timestamps=True` | ✓ PASS |
| `list(segments_gen)` | patched segments | end-time tightening + wordless drop loop | ✓ PASS |

### Test Results

```
tests/test_transcriber.py: 34 passed
Full suite (excl. e2e): 144 passed, 1 pre-existing failure (test_translator.py Windows file-lock — unrelated to this phase)
```

## Automated Checks

- `grep -n "vad_parameters" gensubtitles/core/transcriber.py` → line 139 ✓
- `grep -n '"medium"' gensubtitles/core/transcriber.py` → lines 29, 58, 213 ✓
- `grep -c "medium" README.md` → 2 ✓
- `grep -c "medium" README.es.md` → 2 ✓
- `grep "medium" gensubtitles/gui/main.py` → line 951 ✓
