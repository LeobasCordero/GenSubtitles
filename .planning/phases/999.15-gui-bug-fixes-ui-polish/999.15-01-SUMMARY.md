# Phase 999.15 — Plan 01 Summary

## What Was Built

Applied 5 discrete GUI bug fixes to `gensubtitles/gui/main.py` and added `target_lang` field to `gensubtitles/core/settings.py`.

## Key Files

### Modified
- `gensubtitles/core/settings.py` — Added `target_lang: str = ""` field to `AppSettings` dataclass (backward-compatible via load_settings key filtering)
- `gensubtitles/gui/main.py` — All 5 bug fixes applied

## Bug Fix Details

| Bug | Fix Applied |
|-----|-------------|
| BUG-1 | `_on_generate` hides `_btn_open_folder` via `grid_remove()` at method start |
| BUG-2 | `_build_ui` builds engine dropdown dynamically: Argos always, DeepL/LibreTranslate only if credentials exist; `_save_settings` refreshes dropdown after credential changes |
| BUG-3 | `target_lang` added to AppSettings; `_apply_startup_target_lang()` method restores saved target language on startup; `_on_target_lang_change` auto-saves via `dataclasses.replace` + `save_settings` |
| BUG-4 | `_show_about` CTkToplevel uses `fg_color=_p("bg")`, GitHub button uses `_p("accent")/_p("accent_hov")`, Close button uses `_p("secondary")/_p("secondary_hov")` |
| BUG-5 | `_btn_generate` and `_btn_clear` have `text_color_disabled=("#757575", "#9E9E9E")` for readable disabled state in light theme |

## Deviations from Plan

None. All changes implemented exactly as specified.

## Verification

All automated acceptance checks passed:
- `gensubtitles/core/settings.py` and `gensubtitles/gui/main.py` parse without AST errors
- All 5 bug fix grep checks pass
- `from gensubtitles.core.settings import AppSettings` confirms `target_lang` field present with `""` default

## Self-Check: PASSED
