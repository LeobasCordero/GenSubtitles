---
plan: 999.18-01
phase: 999.18-docs-readme-update
status: complete
completed: 2026-04-13
---

# Summary: 999.18-01 — Rewrite README.md

## What Was Built

Rewrote README.md from scratch with the 9-section audience-split structure defined in D-01. Replaced the old flat CLI-only structure that was missing GUI, translate/convert subcommands, --format/--engine flags, and configuration docs.

## Key Files

### Created
_(none — updated existing file)_

### Modified
- `README.md` — Full rewrite with all 9 sections

## Sections Added / Changed

1. **Installation** — unchanged from previous
2. **Quick Start** — new; links to GUI / CLI / API sections
3. **GUI Usage** — new; 4 sub-sections: Generating Subtitles, Translation Settings, Configuring the App (Settings Dialog), Help Menu
4. **CLI Usage** — restructured; now 4 subcommands with complete flags tables including `--format` and `--engine`
5. **API Usage** — updated server start instructions, same endpoint docs
6. **Configuration** — new; shows settings.json path per OS and all 8 AppSettings fields with defaults
7. **Troubleshooting** — expanded; added DeepL/LibreTranslate GUI limitation note and first-run model size tip
8. **License** — unchanged

## Deviations

- Used `python main.py gui` (the actual CLI subcommand) for the GUI Quick Start entry, rather than `python main.py` (no args), which shows help per `no_args_is_help=True` in the Typer callback. Source code is the source of truth.

## Verification

```
README.md validation passed
```
All automated assertions from PLAN.md Task 1 passed.

## Self-Check: PASSED
