---
phase: 999.18-docs-readme-update
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "README.md has a Quick Start section with three usage modes: GUI, CLI, API"
    - "README.md has a full GUI Usage section with sub-sections for main workflow, translation settings, Settings dialog, and Help menu"
    - "README.md documents all four CLI subcommands: generate (default), translate, convert, serve"
    - "README.md documents --format (srt/ssa) and --engine (argos/deepl/libretranslate) flags for the generate subcommand"
    - "README.md has a Configuration section documenting settings.json location and all 8 fields"
    - "README.md notes the DeepL/LibreTranslate GUI limitation"
    - "README.md replaces the old flat CLI-only structure with the new audience-split structure from D-01"
  artifacts:
    - path: "README.md"
      provides: "Updated English README with all post-v1.0 features documented"
      contains: "## GUI Usage"
  key_links:
    - from: "README.md ## CLI Usage"
      to: "gensubtitles/cli/main.py"
      via: "all subcommands (generate, translate, convert, serve)"
    - from: "README.md ## Configuration"
      to: "gensubtitles/core/settings.py AppSettings"
      via: "8 settings fields with defaults"
---

<objective>
Rewrite README.md with the new audience-split structure defined in D-01.

Purpose: The current README covers only CLI + basic API with a flat structure. Post-v1.0 has added GUI, `translate`/`convert` subcommands, `--format`/`--engine` flags, settings persistence, and SSA output. The README must be fully updated to reflect these additions.

Output: Rewritten README.md with all sections from D-01 (header, installation, quick start, GUI usage, CLI usage, API usage, configuration, troubleshooting, license).
</objective>

<context>
@.planning/phases/999.18-docs-readme-update/999.18-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with new audience-split structure</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state to understand what to preserve and replace)
    - gensubtitles/cli/main.py (source of truth for all CLI subcommands and flags)
    - gensubtitles/core/settings.py (source of truth for AppSettings fields and defaults)
    - gensubtitles/gui/main.py (source of truth for GUI feature surface)
    - .planning/phases/999.18-docs-readme-update/999.18-CONTEXT.md (locked decisions D-01 through D-04)
  </read_first>
  <action>
Replace README.md entirely with the following structure (per D-01):

```
# GenSubtitles
(one-liner + badges if any)

## Installation
(unchanged from current — FFmpeg + Python deps)

## Quick Start
Three usage modes — link each to its section:
- **GUI:** `python main.py` (no args)
- **CLI:** `python main.py --input video.mp4`
- **API:** `python main.py serve`

## GUI Usage
Four sub-sections (per D-02 — inline prose, ~10-20 lines each, no screenshots):

### Generating Subtitles
Step-by-step: launch app → select video → choose model → set language → click Generate.
Note model download on first run.

### Translation Settings
How to pick source/target language from dropdowns on the main form.
Note: Argos Translate is used in GUI (offline). DeepL and LibreTranslate "available via CLI (--engine deepl / --engine libretranslate); GUI support coming in a future release."

### Configuring the App (Settings Dialog)
How to open Settings (menu or button). Document all 8 settings fields from AppSettings:
- Appearance mode: "Light" | "Dark" | "System" (default: "System")
- UI language: "en" | "es" (default: "en")
- Default output directory: absolute path or blank (default: same dir as input)
- Default source language: ISO 639-1 code or blank (auto-detect, default: blank)
- Target language: ISO 639-1 code or blank (no translation, default: blank)
- DeepL API key: Free tier key from deepl.com (default: blank)
- LibreTranslate URL: e.g. "http://localhost:5000" (default: blank)
- LibreTranslate API key: blank = open instance (default: blank)
Settings saved to OS config dir (see Configuration section for path).

### Help Menu
What's in the Help menu: Installed Language Pairs, Tutorial, About.
How to open "Installed Language Pairs" to see which Argos models are downloaded.

## CLI Usage

### generate (default)
`python main.py [OPTIONS]` — generates subtitles from a video file.
Updated flags table including --format and --engine:

| Flag | Description | Default |
|------|-------------|---------|
| `--input`, `-i` | Input video (.mp4, .mkv, .avi, .mov, .webm) | Required |
| `--output`, `-o` | Destination subtitle path | `<input>.<format>` |
| `--model`, `-m` | Whisper model: tiny/base/small/medium/large-v1/v2/v3/turbo | `medium` |
| `--target-lang`, `-t` | Target ISO 639-1 code (e.g. `es`). Omit = no translation | None |
| `--source-lang`, `-s` | Source language code (omit = Whisper auto-detect) | Auto |
| `--device` | Compute device: auto/cpu/cuda | `auto` |
| `--format`, `-f` | Output format: `srt` or `ssa` | `srt` |
| `--engine` | Translation engine: `argos`/`deepl`/`libretranslate` | `argos` |

### translate
`python main.py translate <file> --target-lang <code> [OPTIONS]`
Translates an existing .srt or .ssa subtitle file.

| Argument/Flag | Description | Default |
|------|-------------|---------|
| `<file>` | Input subtitle file (.srt or .ssa) | Required |
| `--target-lang`, `-t` | Target ISO 639-1 code | Required |
| `--source-lang`, `-s` | Source language code | `en` |
| `--output`, `-o` | Output path | `<input>_translated.<ext>` |

### convert
`python main.py convert <input> <output>`
Converts subtitle format (.srt ↔ .ssa). Format inferred from output extension.

### serve
`python main.py serve [--host HOST] [--port PORT] [--reload]`
Starts the FastAPI REST API server.

## API Usage
(same content as current README — POST /subtitles endpoint, query params, interactive docs)

## Configuration
Settings are stored as JSON at:
- **Linux/macOS:** `~/.config/GenSubtitles/settings.json`
- **Windows:** `%APPDATA%\GenSubtitles\settings.json`

List all 8 fields with types and defaults (from AppSettings dataclass).

## Troubleshooting
Update to include:
- FFmpeg not found (keep existing)
- Argos model download failure (keep existing)
- Missing output directory (keep existing)
- Add: "DeepL/LibreTranslate: GUI support not yet active — use `--engine deepl` or `--engine libretranslate` via CLI."
- Add: "First run — large model download: use --model small or --model tiny for a smaller initial download."

## License
MIT (unchanged)
```

Concrete values to use throughout:
- Settings file path (Windows): `%APPDATA%\GenSubtitles\settings.json` — derive from `platformdirs.user_config_dir("GenSubtitles")`
- Settings file path (Linux/macOS): `~/.config/GenSubtitles/settings.json`
- All 8 AppSettings fields + defaults from `gensubtitles/core/settings.py` AppSettings dataclass
- All CLI subcommands + flags from `gensubtitles/cli/main.py` — do NOT invent flags not present in the source
- GUI feature surface confirmed in canonical_refs of CONTEXT.md
  </action>
  <verify>
    <automated>python -c "content = open('README.md').read(); assert '## GUI Usage' in content, 'Missing GUI section'; assert '## Quick Start' in content, 'Missing Quick Start'; assert 'translate' in content, 'Missing translate subcommand'; assert 'convert' in content, 'Missing convert subcommand'; assert '--format' in content, 'Missing --format flag'; assert '--engine' in content, 'Missing --engine flag'; assert 'settings.json' in content, 'Missing settings file doc'; assert 'LibreTranslate' in content, 'Missing LibreTranslate doc'; print('README.md validation passed')"</automated>
  </verify>
  <done>README.md contains all 9 sections from D-01. All CLI subcommands documented. GUI section has 4 sub-sections. Configuration section lists all 8 settings fields with defaults. DeepL/LibreTranslate GUI limitation is noted.</done>
</task>

</tasks>

<verification>
Run the automated verify command from Task 1. Review README.md manually for completeness of:
- GUI Usage section has 4 sub-sections
- CLI flags table includes --format and --engine
- Configuration section lists all 8 AppSettings fields
- Troubleshooting covers the GUI limitation note
</verification>

<success_criteria>
README.md fully documents all post-v1.0 features. A user reading only the README can understand how to use the GUI, all CLI subcommands with new flags, the API, and where to find settings.
</success_criteria>

<output>
After completion, create `.planning/phases/999.18-docs-readme-update/999.18-01-SUMMARY.md`
</output>
