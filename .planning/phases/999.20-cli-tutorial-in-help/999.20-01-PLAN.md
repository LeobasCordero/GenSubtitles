---
phase: 999.20-cli-tutorial-in-help
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cli-tutorial.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "docs/cli-tutorial.md exists with a Prerequisites section at the top"
    - "Tutorial covers generate command: basic, with-translation, format options, engine selection"
    - "Tutorial covers translate, convert, serve, and --step stepper commands"
    - "serve section includes a curl POST /subtitles example"
    - "--step section shows a complete 4-command sequence using --work-dir ./my-job/"
    - "Model comparison is a table (Model | Size | Use when) with qualitative descriptions"
    - "Tutorial ends with a footer cross-link to python main.py gui"
  artifacts:
    - path: "docs/cli-tutorial.md"
      provides: "Standalone CLI tutorial — all commands, step-by-step, with output snippets"
      contains: "## Prerequisites"
    - path: "docs/cli-tutorial.md"
      contains: "## The `--step` Stepper"
    - path: "docs/cli-tutorial.md"
      contains: "POST /subtitles"
  key_links:
    - from: "docs/cli-tutorial.md"
      to: "gensubtitles/cli/main.py"
      via: "tutorial examples must match actual CLI output format strings"
      pattern: "\\[1/4\\] Extracting audio"
    - from: "docs/cli-tutorial.md"
      to: "docs/cli-tutorial.md footer"
      via: "GUI cross-link at bottom"
      pattern: "python main.py gui"
---

<objective>
Create `docs/cli-tutorial.md` — a standalone, step-by-step CLI tutorial for technical users who don't use the GUI.

Purpose: Technical users need a narrative walkthrough from installation through every command, not just the flag reference table that README provides.
Output: `docs/cli-tutorial.md` with ~8 sections covering all commands and flags, ready to link from README and the GUI Help dialog.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.20-cli-tutorial-in-help/999.20-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Key CLI facts extracted from gensubtitles/cli/main.py — executor must use these exactly. -->

Commands: generate (default callback), translate, convert, serve, gui
Progress format: `[{current}/{total}] {label}...`  — e.g. [1/4] Extracting audio...
Done format (generate): `Done: {path} ({segments} segments, lang={lang})`
Done format (stepper transcribe): `Done: {work_dir}/transcription.json ({N} segments, lang={lang})`
Done format (stepper translate): `Done: {work_dir}/translation.json`
Done format (stepper write): `Done: {srt_out}`
Done format (translate subcommand): `Done: {result_path}`
Done format (convert subcommand): `Done: {output_file}`
Serve output: `Starting GenSubtitles API on http://{host}:{port} ...`

--step choices: extract | transcribe | translate | write (requires --work-dir)
--engine choices: argos | deepl | libretranslate
--format choices: srt | ssa
--model choices: tiny | base | small | medium | large-v1 | large-v2 | large-v3 | turbo
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/cli-tutorial.md</name>
  <files>docs/cli-tutorial.md</files>
  <read_first>
    - gensubtitles/cli/main.py — actual command signatures, flags, output strings (executor MUST verify examples match)
    - README.md — existing flag table style (use same bash code block + table pattern)
    - .planning/phases/999.20-cli-tutorial-in-help/999.20-CONTEXT.md — all locked decisions D-01 through D-04
  </read_first>
  <action>
Create `docs/cli-tutorial.md` with the structure described below.

TONE AND STYLE:
- Direct and task-oriented: "Run this, see this output, here's what it means."
- Fenced bash code blocks with `# comment` annotations (same style as README.md).
- Output snippets must use the ACTUAL CLI output format strings from gensubtitles/cli/main.py (read that file first to confirm):
  - Progress: `[{current}/{total}] {label}...` e.g. `[1/4] Extracting audio...`
  - Generate done: `Done: {path} ({segments} segments, lang={lang})`
  - Translate subcommand done: `Done: {result_path}`
  - Convert done: `Done: {output_file}`
  - Serve startup: `Starting GenSubtitles API on http://{host}:{port} ...`
  - Step transcribe done: `Done: {work_dir}/transcription.json ({N} segments, lang={lang})`
  - Step translate done: `Done: {work_dir}/translation.json`
  - Step write done: `Done: {srt_out}`
- DO NOT include numeric timing benchmarks — qualitative descriptions only (per D-03).

DOCUMENT STRUCTURE (8 sections, in order):

SECTION: Title and intro
  Title: `# GenSubtitles — CLI Tutorial`
  Add a blockquote note: `> **GUI user?** Open the desktop app with \`python main.py gui\` instead.`
  One-sentence intro: step-by-step walkthrough, covers every command from first run through advanced pipeline control.

SECTION: `## Prerequisites`
  Three subsections: Python >= 3.11, FFmpeg install (Linux/macOS/Windows + verify), Install GenSubtitles (pip + uv).
  Keep brief — just enough to be self-contained. Add link to README#installation for full details.

SECTION: `## 1. Generate Subtitles (\`generate\`)`
  Subsections: Basic transcription (with expected output), With translation, Custom output path, Format options (SRT vs SSA with note on compatibility), Engine selection (argos/deepl/libretranslate with env var requirements). End with cross-reference to Translation Engines section.

SECTION: `## 2. Translate an Existing Subtitle File (\`translate\`)`
  Show: basic usage, explicit source lang, custom output path. Include expected `Done:` output line.

SECTION: `## 3. Convert Format (\`convert\`)`
  Show SRT to SSA and SSA to SRT. Include expected `Done:` output line. Note format inferred from extension.

SECTION: `## 4. Start the API Server (\`serve\`)`
  Show: default, --host 0.0.0.0, --reload. Include expected startup output line.
  Subsection `### Generating subtitles via the API`: three curl examples — basic, with translation, with format+engine params. Include note about interactive docs at /docs.

SECTION: `## 5. The \`--step\` Stepper`
  Explain: single pipeline stage at a time, work directory, four stages in order.
  Full 4-stage walkthrough using `--work-dir ./my-job/` as the running example:
    Stage 1 extract: `python main.py --input video.mp4 --step extract --work-dir ./my-job/`
    Stage 2 transcribe: `python main.py --step transcribe --work-dir ./my-job/`
    Stage 3 translate: `python main.py --step translate --work-dir ./my-job/ --target-lang es`
    Stage 4 write: `python main.py --step write --work-dir ./my-job/ --output final.srt`
  Show expected output after each command.
  Add a stage options table: Stage | Required flags | Optional flags (for all 4 stages).

SECTION: `## 6. Model Comparison`
  Table: Model | Download size | Use when
  Rows: tiny (~75 MB, fastest/tests), base (~145 MB, simple content), small (~470 MB, conversational), medium (~1.5 GB, default/most content), large-v1/v2/v3 (~3 GB, difficult accents/technical), turbo (~810 MB, near-large accuracy at medium speed).
  Add note about first-run download being automatic (offline after that).

SECTION: `## 7. Translation Engines`
  Table: Engine | Mode | Use when
  Rows: argos (fully offline default), deepl (cloud API, needs DEEPL_API_KEY), libretranslate (self-hosted, needs LIBRETRANSLATE_URL).

SECTION: `## 8. Quick Reference`
  Single bash block with 8 most common invocations, one per line with inline comment.

FOOTER:
  Blockquote cross-link: `> **Prefer a graphical interface?** Run \`python main.py gui\` to open the desktop app. The GUI Tutorial (Help > Tutorial) covers all GUI features step by step.`
  </action>
  <verify>
    <automated>python -c "import pathlib; txt = pathlib.Path('docs/cli-tutorial.md').read_text(); checks = ['## Prerequisites', '## 1. Generate', '## 2. Translate', '## 3. Convert', '## 4. Start the API Server', '## 5. The `--step` Stepper', '## 6. Model Comparison', 'POST /subtitles', '--work-dir ./my-job/', 'python main.py gui', '[1/4] Extracting audio', 'Done: video.srt']; missing = [c for c in checks if c not in txt]; print('MISSING:', missing) if missing else print('ALL CHECKS PASS')"</automated>
  </verify>
  <done>docs/cli-tutorial.md exists, all 8 sections present, curl /subtitles example included, stepper 4-stage sequence complete, model comparison table present, footer GUI cross-link present, output snippets match actual CLI format strings from cli/main.py.</done>
</task>

</tasks>

<verification>
Run the automated verify command above. All checks must pass — no MISSING items.
Spot-check: confirm [1/4] Extracting audio... matches gensubtitles/cli/main.py output format.
</verification>

<success_criteria>
- docs/cli-tutorial.md created with all required sections
- Output snippets match actual CLI output format strings
- Model comparison uses qualitative descriptions only (no numeric timings)
- All 6 commands covered: generate, translate, convert, serve, --step, gui mention in footer
- curl example for POST /subtitles included in serve section
- Footer cross-link to `python main.py gui` present
</success_criteria>

<output>
After completion, create `.planning/phases/999.20-cli-tutorial-in-help/999.20-01-SUMMARY.md`
</output>
