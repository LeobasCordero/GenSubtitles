---
phase: 999.20-cli-tutorial-in-help
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.es.md
  - gensubtitles/gui/locale.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "README.md 'CLI Usage' section has a one-line link to docs/cli-tutorial.md"
    - "README.es.md 'Uso de CLI' section has a one-line link to docs/cli-tutorial.md"
    - "locale.py EN dlg_tutorial_text ends with a CLI tutorial cross-link line"
    - "locale.py ES dlg_tutorial_text ends with a CLI tutorial cross-link line in Spanish"
  artifacts:
    - path: "README.md"
      provides: "Cross-link from CLI Usage section to docs/cli-tutorial.md"
      contains: "docs/cli-tutorial.md"
    - path: "README.es.md"
      provides: "Cross-link from Uso de CLI section to docs/cli-tutorial.md"
      contains: "docs/cli-tutorial.md"
    - path: "gensubtitles/gui/locale.py"
      provides: "Cross-link in both EN and ES GUI Tutorial dialog"
      contains: "docs/cli-tutorial.md"
  key_links:
    - from: "README.md §CLI Usage"
      to: "docs/cli-tutorial.md"
      via: "markdown hyperlink after section header"
      pattern: "\\[.*cli-tutorial.*\\]\\(docs/cli-tutorial\\.md\\)"
    - from: "locale.py dlg_tutorial_text EN"
      to: "docs/cli-tutorial.md"
      via: "appended line in Python string"
      pattern: "cli-tutorial\\.md"
    - from: "locale.py dlg_tutorial_text ES"
      to: "docs/cli-tutorial.md"
      via: "appended line in Python string"
      pattern: "cli-tutorial\\.md"
---

<objective>
Add cross-links to `docs/cli-tutorial.md` in three places: README.md, README.es.md, and the GUI Tutorial dialog (locale.py — EN and ES strings).

Purpose: Technical users discovering the CLI via README or via the GUI Help menu need a path to the full tutorial.
Output: Three files updated with minimal targeted edits — no content removed, only a one-liner added to each location.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.20-cli-tutorial-in-help/999.20-CONTEXT.md

<interfaces>
<!-- Exact insertion targets — executor MUST read each file to confirm line numbers before editing. -->

README.md:
  - "## CLI Usage" heading at ~line 102
  - Add ONE line immediately after the heading (before "### generate (default)")
  - Exact text to insert: "> For a full step-by-step walkthrough, see [CLI Tutorial](docs/cli-tutorial.md)."

README.es.md:
  - "## Uso de CLI" heading at ~line 100
  - Add ONE line immediately after the heading (before "### generate (predeterminado)")
  - Exact text to insert: "> Para una guía paso a paso completa, consulta el [Tutorial de CLI](docs/cli-tutorial.md)."

locale.py EN dlg_tutorial_text (line ~170):
  - Currently ends: "\u2022 API connection refused \u2014 The background server failed to start. Restart the application."
  - Change that last string segment to append TWO more lines:
    "\u2022 API connection refused \u2014 The background server failed to start. Restart the application.\n"
    "\n"
    "CLI users: see docs/cli-tutorial.md for a step-by-step terminal walkthrough."

locale.py ES dlg_tutorial_text (line ~321):
  - Currently ends: "\u2022 Conexi\u00f3n a la API rechazada \u2014 El servidor en segundo plano no se pudo iniciar. Reinicia la aplicaci\u00f3n."
  - Change that last string segment to append TWO more lines:
    "\u2022 Conexi\u00f3n a la API rechazada \u2014 El servidor en segundo plano no se pudo iniciar. Reinicia la aplicaci\u00f3n.\n"
    "\n"
    "Usuarios de CLI: consulta docs/cli-tutorial.md para una gu\u00eda paso a paso en terminal."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI tutorial link to README.md and README.es.md</name>
  <files>README.md, README.es.md</files>
  <read_first>
    - README.md — find "## CLI Usage" and the line(s) immediately after it; confirm exact surrounding text
    - README.es.md — find "## Uso de CLI" and the line(s) immediately after it; confirm exact surrounding text
  </read_first>
  <action>
In README.md, find the "## CLI Usage" heading. Insert exactly ONE blank line and then a blockquote link immediately after it, before "### generate (default)":

```
## CLI Usage

> For a full step-by-step walkthrough, see [CLI Tutorial](docs/cli-tutorial.md).

### generate (default)
```

In README.es.md, find the "## Uso de CLI" heading. Insert exactly ONE blank line and then a blockquote link immediately after it, before "### generate (predeterminado)":

```
## Uso de CLI

> Para una guía paso a paso completa, consulta el [Tutorial de CLI](docs/cli-tutorial.md).

### generate (predeterminado)
```

Do NOT modify any other content in either file.
  </action>
  <verify>
    <automated>python -c "import pathlib; en = pathlib.Path('README.md').read_text(); es = pathlib.Path('README.es.md').read_text(); checks = [('README.md', 'docs/cli-tutorial.md', en), ('README.es.md', 'docs/cli-tutorial.md', es)]; [print(f'MISSING in {f}: {needle}') for f, needle, txt in checks if needle not in txt] or print('ALL CHECKS PASS')"</automated>
  </verify>
  <done>README.md contains a link to docs/cli-tutorial.md in the CLI Usage section. README.es.md contains a link to docs/cli-tutorial.md in the Uso de CLI section. No other content changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI tutorial cross-link to locale.py GUI Tutorial dialog (EN + ES)</name>
  <files>gensubtitles/gui/locale.py</files>
  <read_first>
    - gensubtitles/gui/locale.py — read the EN dlg_tutorial_text ending (around line 170) and ES dlg_tutorial_text ending (around line 321) to get the EXACT current string before modifying
  </read_first>
  <action>
In gensubtitles/gui/locale.py, make TWO targeted edits:

**Edit 1 — EN dlg_tutorial_text** (around line 170):

Find this exact string (the last line of the EN dlg_tutorial_text):
```python
            "\u2022 API connection refused \u2014 The background server failed to start. Restart the application."
        ),
```

Replace with:
```python
            "\u2022 API connection refused \u2014 The background server failed to start. Restart the application.\n"
            "\n"
            "CLI users: see docs/cli-tutorial.md for a step-by-step terminal walkthrough."
        ),
```

**Edit 2 — ES dlg_tutorial_text** (around line 321):

Find this exact string (the last line of the ES dlg_tutorial_text):
```python
            "\u2022 Conexi\u00f3n a la API rechazada \u2014 El servidor en segundo plano no se pudo iniciar. Reinicia la aplicaci\u00f3n."
        ),
```

Replace with:
```python
            "\u2022 Conexi\u00f3n a la API rechazada \u2014 El servidor en segundo plano no se pudo iniciar. Reinicia la aplicaci\u00f3n.\n"
            "\n"
            "Usuarios de CLI: consulta docs/cli-tutorial.md para una gu\u00eda paso a paso en terminal."
        ),
```

Do NOT modify any other strings in locale.py. The changes are purely additive — appending to the end of each dialog string.
  </action>
  <verify>
    <automated>python -c "
import pathlib
txt = pathlib.Path('gensubtitles/gui/locale.py').read_text()
checks = [
    'CLI users: see docs/cli-tutorial.md',
    'Usuarios de CLI: consulta docs/cli-tutorial.md',
]
missing = [c for c in checks if c not in txt]
print('MISSING:', missing) if missing else print('ALL CHECKS PASS')
"</automated>
  </verify>
  <done>locale.py EN dlg_tutorial_text contains "CLI users: see docs/cli-tutorial.md for a step-by-step terminal walkthrough." appended after the API connection refused line. locale.py ES dlg_tutorial_text contains "Usuarios de CLI: consulta docs/cli-tutorial.md para una guía paso a paso en terminal." appended after the Spanish API connection refused line.</done>
</task>

</tasks>

<verification>
Run both automated verify commands above — all must print ALL CHECKS PASS.
Spot-check locale.py: confirm no Python syntax was broken — run `python -c "import gensubtitles.gui.locale"` (should produce no output/error).
</verification>

<success_criteria>
- README.md has a blockquote link to docs/cli-tutorial.md under "## CLI Usage"
- README.es.md has a blockquote link to docs/cli-tutorial.md under "## Uso de CLI"
- locale.py EN dlg_tutorial_text ends with CLI tutorial cross-link
- locale.py ES dlg_tutorial_text ends with Spanish CLI tutorial cross-link
- locale.py imports without error
</success_criteria>

<output>
After completion, create `.planning/phases/999.20-cli-tutorial-in-help/999.20-02-SUMMARY.md`
</output>
