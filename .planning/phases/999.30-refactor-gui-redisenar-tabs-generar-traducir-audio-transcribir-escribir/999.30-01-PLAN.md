---
phase: 999.30-refactor-gui-redisenar-tabs-generar-traducir-audio-transcribir-escribir
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gensubtitles/gui/locale.py
  - gensubtitles/gui/main.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "locale.py has new keys for tabs 3-6 names, tab 1 new buttons, and all step tab widgets in both en and es"
    - "_log_to(textbox, msg) helper exists and _log() delegates to it"
    - "_get_tab_work_dir(input_var, fallback_vars) helper exists"
    - "CTkTabview in _build_ui() registers 6 tabs: 'Generate Subtitles', 'Translate Subtitles', 'Extract Audio', 'Transcribe', 'Translate', 'Write Subtitle'"
    - "_relabel_tabview_headers() includes all 6 tab names (en + es sources)"
    - "New StringVars for tabs 3-6 are declared in __init__"
  artifacts:
    - path: "gensubtitles/gui/locale.py"
      provides: "New locale keys for tabs 3-6 and new tab 1 elements"
      contains: "extract_tab"
    - path: "gensubtitles/gui/main.py"
      provides: "Foundation helpers and 6-tab scaffolding"
      contains: "_log_to"
  key_links:
    - from: "gensubtitles/gui/main.py"
      to: "gensubtitles/gui/locale.py"
      via: "s() calls for new keys"
      pattern: "s\\(\"extract_tab\""
---

<objective>
Add all new locale strings and foundation helpers required by the 6-tab refactor without changing existing behavior.

Purpose: Establishes the shared vocabulary (locale keys, StringVars, helper methods, CTkTabview tab registrations) that Plans 02 and 03 depend on. Zero visual change to the running app — all 4 new tabs are registered but empty until Plan 03 builds them.

Output:
- `locale.py` — new locale keys for tabs 3-6 and new tab 1 elements
- `main.py` — `_log_to()`, `_get_tab_work_dir()` helpers; new StringVars; 4 new CTkTabview tab entries; extended `_relabel_tabview_headers()`
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.30-refactor-gui-redisenar-tabs-generar-traducir-audio-transcribir-escribir/999.30-CONTEXT.md
@.planning/phases/999.30-refactor-gui-redisenar-tabs-generar-traducir-audio-transcribir-escribir/999.30-RESEARCH.md

<interfaces>
<!-- Key interfaces for the executor -->

From gensubtitles/gui/locale.py:
```python
# Current _STRINGS["en"] keys (relevant):
"generate_tab":         "Generate Subtitles",
"translate_tab":        "Translate Subtitles",
"clear_btn":            "Clear",
# (many more existing keys)

# locale.py exports:
def s(key: str) -> str: ...
def set_language(lang: str) -> None: ...
def s_lang(key: str, lang: str) -> str: ...
LANGUAGES: tuple = ...
```

From gensubtitles/gui/main.py (existing __init__ StringVars):
```python
self._input_var = ctk.StringVar()
self._output_var = ctk.StringVar()
self._source_lang_var = ctk.StringVar()
self._target_lang_var = ctk.StringVar(value="No target")
self._output_format_var = ctk.StringVar(value="SRT")
self._engine_var = ctk.StringVar(value="Argos")
# Stepper vars (kept for now — Plan 02 removes them):
self._work_dir_var = ctk.StringVar()
self._stepper_switch_var = ctk.BooleanVar(value=False)
```

From gensubtitles/gui/main.py (_log and _build_ui):
```python
def _log(self, msg: str) -> None:
    """Append a message to the activity log textbox with auto-scroll."""
    try:
        self._log_textbox.configure(state="normal")
        self._log_textbox.insert("end", msg + "\n")
        self._log_textbox.configure(state="disabled")
        self._log_textbox.see("end")
    except Exception:
        pass

def _build_ui(self) -> None:
    self._tabview = ctk.CTkTabview(self)
    self._tabview.pack(fill="both", expand=True, padx=16, pady=16)
    self._tabview.add("Generate Subtitles")
    self._tabview.add("Translate Subtitles")
    # ... builds Tab 1 (3-panel) and calls _build_translate_tab()

def _relabel_tabview_headers(self) -> None:
    tab_name_map = {
        "Generate Subtitles": s("generate_tab"),
        "Translate Subtitles": s("translate_tab"),
        s_lang("generate_tab", "es"): s("generate_tab"),
        s_lang("translate_tab", "es"): s("translate_tab"),
    }
    # ... rest of method unchanged
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new locale keys to locale.py</name>
  <files>gensubtitles/gui/locale.py</files>
  <read_first>
    - gensubtitles/gui/locale.py — read the entire file to see existing key structure and placement (current _STRINGS["en"] and _STRINGS["es"] dicts)
  </read_first>
  <action>
Add the following new locale keys to BOTH `_STRINGS["en"]` and `_STRINGS["es"]` dicts in `gensubtitles/gui/locale.py`. Insert as a new comment-separated block after the existing keys in each dict.

**In `_STRINGS["en"]` — add after the existing blocks:**
```python
# Phase 999.30 — 6-tab refactor
# Tab names
"extract_tab":              "Extract Audio",
"transcribe_tab":           "Transcribe",
"translate_step_tab":       "Translate",
"write_tab":                "Write Subtitle",
# Tab 1 new elements
"panel_config_title":       "Configuration",
"generate_clear_btn":       "Clear fields",
# Tab 3 — Extract Audio
"extract_input_lbl":        "Input video *:",
"extract_output_lbl":       "Output audio file:",
"extract_run_btn":          "Extract Audio",
# Tab 4 — Transcribe
"transcribe_input_lbl":     "Input audio *:",
"transcribe_run_btn":       "Transcribe",
# Tab 5 — Translate (step)
"translate_step_input_lbl": "Input transcription *:",
"translate_step_run_btn":   "Translate",
# Tab 6 — Write Subtitle
"write_input_lbl":          "Input transcription *:",
"write_output_lbl":         "Output subtitle *:",
"write_run_btn":            "Write Subtitle",
```

**In `_STRINGS["es"]` — add the same block translated:**
```python
# Phase 999.30 — 6-tab refactor
# Nombres de tabs
"extract_tab":              "Extraer Audio",
"transcribe_tab":           "Transcribir",
"translate_step_tab":       "Traducción",
"write_tab":                "Escribir Subtítulo",
# Tab 1 nuevos elementos
"panel_config_title":       "Configuración",
"generate_clear_btn":       "Limpiar campos",
# Tab 3 — Extraer Audio
"extract_input_lbl":        "Video de entrada *:",
"extract_output_lbl":       "Archivo de audio de salida:",
"extract_run_btn":          "Extraer Audio",
# Tab 4 — Transcribir
"transcribe_input_lbl":     "Audio de entrada *:",
"transcribe_run_btn":       "Transcribir",
# Tab 5 — Traducción (paso)
"translate_step_input_lbl": "Transcripción de entrada *:",
"translate_step_run_btn":   "Traducir",
# Tab 6 — Escribir Subtítulo
"write_input_lbl":          "Transcripción de entrada *:",
"write_output_lbl":         "Subtítulo de salida *:",
"write_run_btn":            "Escribir Subtítulo",
```
  </action>
  <verify>
    <automated>python -c "from gensubtitles.gui.locale import s, set_language; set_language('en'); print(s('extract_tab'), s('write_tab'), s('generate_clear_btn')); set_language('es'); print(s('extract_tab'), s('write_tab'), s('generate_clear_btn'))"</automated>
  </verify>
  <done>Command prints "Extract Audio Write Subtitle Clear fields" and "Extraer Audio Escribir Subtítulo Limpiar campos" without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add StringVars, helpers, and extend _build_ui / _relabel_tabview_headers in main.py</name>
  <files>gensubtitles/gui/main.py</files>
  <read_first>
    - gensubtitles/gui/main.py — read __init__ (lines 120–175), _log() (~567), _build_ui() tab setup (~165–190), _relabel_tabview_headers() (~1957–2010)
  </read_first>
  <action>
Make four targeted additions to `gensubtitles/gui/main.py`:

**A. New StringVars in `__init__` — add after `self._engine_var` declaration (around line 155):**
```python
# Phase 999.30 — per-tab StringVars for step tabs 3-6
self._extract_input_var = ctk.StringVar()
self._extract_output_var = ctk.StringVar()
self._transcribe_input_var = ctk.StringVar()
self._translate_step_input_var = ctk.StringVar()
self._translate_step_source_var = ctk.StringVar()
self._translate_step_target_var = ctk.StringVar(value="No target")
self._translate_step_engine_var = ctk.StringVar(value="Argos")
self._write_input_var = ctk.StringVar()
self._write_output_var = ctk.StringVar()
self._write_format_var = ctk.StringVar(value="SRT")
```

**B. New helper methods — add immediately after the existing `_log(self, msg)` method (after line ~576):**
```python
def _log_to(self, textbox: "ctk.CTkTextbox", msg: str) -> None:
    """Append *msg* to any CTkTextbox log widget with auto-scroll."""
    try:
        textbox.configure(state="normal")
        textbox.insert("end", msg + "\n")
        textbox.configure(state="disabled")
        textbox.see("end")
    except Exception:  # noqa: BLE001
        pass

def _get_tab_work_dir(
    self,
    input_var: "ctk.StringVar",
    fallback_vars: "list[ctk.StringVar] | None" = None,
) -> "Path | None":
    """Return work-dir path derived from *input_var*, or first non-empty fallback.

    Fallback chain (D-05): tab's own input → _input_var (Tab 1 video) → _extract_input_var (Tab 3 audio).
    """
    val = input_var.get().strip()
    if val:
        return Path(val).parent
    if fallback_vars:
        for fv in fallback_vars:
            fv_val = fv.get().strip()
            if fv_val:
                return Path(fv_val).parent
    return None
```

Also update the existing `_log` method body to delegate to `_log_to`:
```python
def _log(self, msg: str) -> None:
    """Append a message to the Tab 1 activity log textbox with auto-scroll."""
    self._log_to(self._log_textbox, msg)
```

**C. Register 4 new CTkTabview tabs in `_build_ui()` — add immediately after `self._tabview.add("Translate Subtitles")` (around line 188):**
```python
self._tabview.add("Extract Audio")    # Tab 3 — built by Plan 03
self._tabview.add("Transcribe")       # Tab 4 — built by Plan 03
self._tabview.add("Translate")        # Tab 5 — built by Plan 03
self._tabview.add("Write Subtitle")   # Tab 6 — built by Plan 03
```

**D. Extend `_relabel_tabview_headers()` — add 4 new tab mappings to `tab_name_map` (inside the existing dict literal):**
```python
tab_name_map = {
    "Generate Subtitles":    s("generate_tab"),
    "Translate Subtitles":   s("translate_tab"),
    # Phase 999.30 — new tabs
    "Extract Audio":         s("extract_tab"),
    "Transcribe":            s("transcribe_tab"),
    "Translate":             s("translate_step_tab"),
    "Write Subtitle":        s("write_tab"),
    # Localized sources — EN
    s_lang("generate_tab",       "en"): s("generate_tab"),
    s_lang("translate_tab",      "en"): s("translate_tab"),
    s_lang("extract_tab",        "en"): s("extract_tab"),
    s_lang("transcribe_tab",     "en"): s("transcribe_tab"),
    s_lang("translate_step_tab", "en"): s("translate_step_tab"),
    s_lang("write_tab",          "en"): s("write_tab"),
    # Localized sources — ES
    s_lang("generate_tab",       "es"): s("generate_tab"),
    s_lang("translate_tab",      "es"): s("translate_tab"),
    s_lang("extract_tab",        "es"): s("extract_tab"),
    s_lang("transcribe_tab",     "es"): s("transcribe_tab"),
    s_lang("translate_step_tab", "es"): s("translate_step_tab"),
    s_lang("write_tab",          "es"): s("write_tab"),
}
```
  </action>
  <verify>
    <automated>python -c "from gensubtitles.gui.main import GenSubtitlesApp; print('import OK')"</automated>
  </verify>
  <done>
    - `python -c "from gensubtitles.gui.main import GenSubtitlesApp; print('import OK')"` exits 0
    - `grep -n "_log_to\|_get_tab_work_dir" gensubtitles/gui/main.py` shows both methods defined
    - `grep -n "_extract_input_var\|_write_format_var" gensubtitles/gui/main.py` shows new StringVars in __init__
    - `grep -n '"Extract Audio"\|"Transcribe"\|"Write Subtitle"' gensubtitles/gui/main.py` shows all 4 new tab registrations
    - `grep -n "extract_tab\|write_tab" gensubtitles/gui/main.py` shows entries in _relabel_tabview_headers
  </done>
</task>

</tasks>

<verification>
```bash
python -c "from gensubtitles.gui.main import GenSubtitlesApp; print('import OK')"
python -c "from gensubtitles.gui.locale import s, set_language; [set_language(l) or print(s('extract_tab'), s('write_tab')) for l in ('en','es')]"
grep -n "_log_to\|_get_tab_work_dir" gensubtitles/gui/main.py
grep -c "extract_tab\|transcribe_tab\|translate_step_tab\|write_tab" gensubtitles/gui/locale.py
```
</verification>

<success_criteria>
- locale.py contains 18 new keys (extract_tab, transcribe_tab, translate_step_tab, write_tab, panel_config_title, generate_clear_btn + 12 widget label keys) in both en and es dicts
- main.py module imports without error (headless)
- `_log_to()` defined; `_log()` delegates to `_log_to(self._log_textbox, msg)`
- `_get_tab_work_dir()` defined with input_var + fallback_vars params
- 10 new StringVars declared in `__init__`
- CTkTabview in `_build_ui()` registers 6 tabs (verified by grep)
- `_relabel_tabview_headers()` maps all 6 tab names in both languages
</success_criteria>

<output>
After completion, create `.planning/phases/999.30-refactor-gui-redisenar-tabs-generar-traducir-audio-transcribir-escribir/999.30-01-SUMMARY.md`
</output>
