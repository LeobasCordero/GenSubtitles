# Summary: 999.30-02 — Rebuild Tab 1 to 2-panel layout, remove stepper code

## Status: COMPLETE

## What Was Built

**Tab 1 rebuilt from 3-panel to 2-panel layout (D-02):**
- `_files_frame` (col 0): input video + output file entries with browse buttons (no title, no clear button)
- `_config_frame` (col 1): "Configuration" title label, 4 dropdowns (source lang, target lang, engine [hidden], output format), Generate button (accent), Clear fields button (secondary, uses new `generate_clear_btn` locale key), stage label, log textbox (120px), elapsed label (hidden), progress bar (hidden), cancel button (hidden)
- `minsize` changed from `(1100, 600)` to `(900, 560)`

**Stepper code removed:**
- `__init__`: removed 6 stepper vars (`_work_dir_var`, `_stepper_switch_var`, `_step_states`, `_stepper_refresh_id`, `_work_dir_from_browse`, `_work_dir_browse_just_set`)
- `on_closing()`: removed stepper refresh cancellation block
- Deleted methods: `_build_stepper_section`, `_browse_work_dir`, `_on_work_dir_changed`, `_on_stepper_switch_changed`, `_schedule_stepper_refresh`, `_do_stepper_refresh`, `_refresh_stepper_state`, `_get_effective_work_dir`, `_on_clear_work`
- `_show_success()`: moved `_btn_open_folder` from deleted `_status_frame` to `_config_frame` at row 16

**`_apply_theme`**: Added log textbox theming loop using `getattr` pattern for all 5 log textbox attributes.

**`_apply_ui_language`**: Removed all stepper widget references; updated generate button to always use `"generate_btn"` text; added `_lbl_config_title` and `_btn_clear` (using `generate_clear_btn`) references.

## Key Files

- `gensubtitles/gui/main.py` — full Tab 1 section rewritten (~lines 196-390)

## Commit

`0ad59c7` feat(999.30-02): rebuild Tab 1 to 2-panel layout, remove all stepper code

## Deviations

- Also fixed `_show_success()` which referenced deleted `_status_frame` — changed to `_config_frame` at row 16 to avoid runtime AttributeError when generation completes.
