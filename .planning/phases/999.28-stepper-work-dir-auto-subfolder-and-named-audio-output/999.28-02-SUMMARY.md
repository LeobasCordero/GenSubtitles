---
plan: 999.28-02
status: complete
commit: ea93b04
---

# Plan 999.28-02 Summary

## What Was Built

- **`_work_dir_from_browse: bool`** and **`_work_dir_browse_just_set: bool`** instance variables added to the stepper state block
- **`_browse_work_dir()`** updated to set both flags before assigning the var (sentinel pattern prevents trace from clearing the browse flag)
- **`_on_work_dir_changed()`** new method replacing the old lambda trace — consumes the sentinel on Browse-originated writes; resets `_work_dir_from_browse` on manual edits (per D-05)
- **`_get_effective_work_dir()`** new method — when `_work_dir_from_browse=True` and a video is loaded, returns `parent / sanitize_stem(stem)` subfolder; otherwise returns the raw path as-is
- **`_on_step_extract()`** updated to use `_get_effective_work_dir()`, creates the subfolder via `mkdir(parents=True, exist_ok=True)`, passes effective path to the API
- **`_refresh_stepper_state()`** updated to use `_get_effective_work_dir()` and `any(work_dir.glob("*.wav"))` for audio presence check (per D-03)
- **`_on_clear_work()`** updated to use `_get_effective_work_dir()` and globs `*.wav` for deletion (no hardcoded `audio.wav`)

## Key Files

- `gensubtitles/gui/main.py` — all stepper changes

## Test Results

- `test_steps.py`: 19/19 passed
- `test_api_steps.py` + `test_api.py`: 28/28 passed
- `gui/main.py`: syntax verified clean

## Deviations

None. Pre-existing `test_settings_path_expands_user` failure on Windows is unrelated to this work.
