---
plan: 999.21-01
phase: 999.21-gui-refactor-palette-colors
status: complete
completed: 2026-04-14
commit: fda8856
---

## Summary

Extracted the dual-mode colour palette and typography scale from `gensubtitles/gui/main.py` into a new dedicated module `gensubtitles/gui/theme.py`. All ~90 `_p(` call sites and ~7 `_font(` call sites were renamed to `p(` and `font(` respectively.

## What Was Built

- **`gensubtitles/gui/theme.py`** (new) — Contains `_PALETTES`, `p()`, `_FONT_SIZES`, `_FONT_FAMILIES`, `_BOLD_ROLES`, `font()`. Public API: `p(key: str) -> str` and `font(role: str = "body") -> ctk.CTkFont`.
- **`gensubtitles/gui/main.py`** (modified) — Removed lines 31–137 (theme definitions), added `from .theme import font, p`, renamed all call sites from `_p(` → `p(` and `_font(` → `font(`.

## Key Files

key-files:
  created:
    - gensubtitles/gui/theme.py
  modified:
    - gensubtitles/gui/main.py

## Verification

- `theme.py` syntax: OK
- `main.py` syntax: OK  
- Zero `_p(` or `_font(` occurrences in `main.py`: PASS
- `from .theme import font, p` present: PASS
- Test suite (excluding pre-existing failures): 165 passed, 0 new failures

## Deviations

None. Implementation matches plan exactly.

## Self-Check: PASSED
