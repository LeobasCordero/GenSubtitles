---
phase: 11-retroactive-verification-core-modules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-translation-engine/04-VERIFICATION.md
  - .planning/phases/05-srt-generation-module/05-VERIFICATION.md
  - .planning/phases/05-srt-generation-module/05-VALIDATION.md
autonomous: true
requirements: [TRANS-01, TRANS-02, TRANS-03, TRANS-04, TRANS-05, SRT-01, SRT-02, SRT-03, SRT-04]

must_haves:
  truths:
    - "Phase 04 VERIFICATION.md exists with status: passed and all 5 TRANS-xx requirement IDs listed as verified"
    - "Phase 05 VERIFICATION.md exists with status: passed and all 4 SRT-xx requirement IDs listed as verified"
    - "05-VALIDATION.md is deleted (archived) per D-03 — it is superseded"
  artifacts:
    - path: ".planning/phases/04-translation-engine/04-VERIFICATION.md"
      provides: "Formal exit record for Phase 04 (Translation Engine)"
      contains: "TRANS-01"
    - path: ".planning/phases/05-srt-generation-module/05-VERIFICATION.md"
      provides: "Formal exit record for Phase 05 (SRT Generation Module)"
      contains: "SRT-01"
  key_links:
    - from: "04-VERIFICATION.md"
      to: "04-UAT.md"
      via: "evidence citation"
      pattern: "04-UAT\\.md"
    - from: "05-VERIFICATION.md"
      to: "05-UAT.md"
      via: "evidence citation"
      pattern: "05-UAT\\.md"
---

<objective>
Write VERIFICATION.md formal exit records for Phase 04 (Translation Engine) and Phase 05 (SRT Generation Module), then archive the superseded 05-VALIDATION.md.

Purpose: Close the verification gate for phases 04 and 05 by creating VERIFICATION.md files that formally record requirement coverage. All code is complete and UAT results are already captured — this is a documentation artifact only.

Output:
- `.planning/phases/04-translation-engine/04-VERIFICATION.md`
- `.planning/phases/05-srt-generation-module/05-VERIFICATION.md`
- `.planning/phases/05-srt-generation-module/05-VALIDATION.md` deleted
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-retroactive-verification-core-modules/11-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Template and evidence sources — MUST read before writing VERIFICATION.md files -->
@.planning/phases/10-documentation-end-to-end-validation/10-VERIFICATION.md
@.planning/phases/04-translation-engine/04-UAT.md
@.planning/phases/05-srt-generation-module/05-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 04 VERIFICATION.md</name>
  <files>
    .planning/phases/04-translation-engine/04-VERIFICATION.md
  </files>

  <read_first>
    - `.planning/phases/10-documentation-end-to-end-validation/10-VERIFICATION.md` — canonical format template (YAML frontmatter + Truths Verified + Artifacts Verified + Key Links Verified sections)
    - `.planning/phases/04-translation-engine/04-UAT.md` — primary evidence source (4 UAT tests, all pass; 18 unit tests pass)
  </read_first>

  <action>
Create `.planning/phases/04-translation-engine/04-VERIFICATION.md` following the same YAML frontmatter + section structure as `10-VERIFICATION.md` (D-05).

Frontmatter (lines 1–7 of the file): phase=04-translation-engine, status=passed, verified=2026-04-20, verification_method=inspection + automated, score=5/5.

File body:
```

# Phase 04 Verification Report

**Phase:** 04 - Translation Engine
**Status:** ✅ PASSED
**Date:** 2026-04-20

## Must-Have Verification

### Truths Verified

1. ✅ **Transcription translated offline using Argos Translate (TRANS-01)**
   - `ArgosTranslator` in `gensubtitles/core/translator.py` uses `argostranslate` library
   - No external API calls — fully offline after initial model download
   - Verified by inspection: `core/translator.py` imports `argostranslate`

2. ✅ **Translation is optional — skipped when source == target language (TRANS-02)**
   - `translate_segments()` returns original segments unchanged when `source_lang == target_lang`
   - No Argos calls made in no-op path
   - Verified by test #2 in 04-UAT.md
   - Evidence: `tests/test_translator.py` (18 unit tests passing)

3. ✅ **Language model packages installed programmatically on first use (TRANS-03)**
   - `ArgosTranslator` installs language pack on demand via `argostranslate.package`
   - No manual model download step required
   - Verified by inspection: `core/translator.py`

4. ✅ **Available language pairs listed; unsupported pairs fail gracefully (TRANS-04)**
   - `translate_segments()` raises `ValueError` with pair name when pair is unsupported
   - Verified by test #3 in 04-UAT.md (e.g. "en"→"tlh" raises ValueError)
   - Evidence: `tests/test_translator.py`

5. ✅ **Models downloaded once and cached locally (TRANS-05)**
   - Argos Translate caches downloaded models in user data directory
   - No re-download on subsequent runs
   - Verified by inspection: `core/translator.py` uses `argostranslate.package.get_installed_packages()`

### Artifacts Verified

1. ✅ **gensubtitles/core/translator.py**
   - Contains `ArgosTranslator` class (TRANS-01, TRANS-02, TRANS-03, TRANS-04, TRANS-05)
   - `translate_segments()` returns `TranslatedSegment` objects with `.start`, `.end`, `.text`
   - Compatible with Phase 05 SRT writer (verified by test #4 in 04-UAT.md)

2. ✅ **tests/test_translator.py**
   - 18 unit tests, all passing (see 04-UAT.md)
   - No internet connection or Argos models required to run tests

### Key Links Verified

1. ✅ **translate_segments() → argostranslate**
   - Same-language no-op path returns original segments without calling Argos (TRANS-02)
   - Unsupported pair raises ValueError before Argos is invoked (TRANS-04)

2. ✅ **TranslatedSegment → SRT writer compatibility**
   - `TranslatedSegment` has `.start`, `.end`, `.text` attributes
   - Compatible with Phase 05 `write_srt()` input contract (TRANS-05 downstream)
   - Verified by test #4 in 04-UAT.md
```
  </action>

  <verify>
    <automated>Test-Path ".planning/phases/04-translation-engine/04-VERIFICATION.md" ; (Get-Content ".planning/phases/04-translation-engine/04-VERIFICATION.md" | Select-String "status: passed")</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/04-translation-engine/04-VERIFICATION.md` exists
    - File contains `status: passed` in YAML frontmatter
    - File contains `score: 5/5`
    - File contains all 5 requirement IDs: `TRANS-01`, `TRANS-02`, `TRANS-03`, `TRANS-04`, `TRANS-05`
    - File contains `tests/test_translator.py` as evidence citation
    - File contains `04-UAT.md` reference
  </acceptance_criteria>

  <done>04-VERIFICATION.md exists with status: passed, all 5 TRANS-xx IDs present.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 05 VERIFICATION.md and archive 05-VALIDATION.md</name>
  <files>
    .planning/phases/05-srt-generation-module/05-VERIFICATION.md
    .planning/phases/05-srt-generation-module/05-VALIDATION.md
  </files>

  <read_first>
    - `.planning/phases/10-documentation-end-to-end-validation/10-VERIFICATION.md` — canonical format template
    - `.planning/phases/05-srt-generation-module/05-UAT.md` — primary evidence source (4 UAT tests, all pass; 14 unit tests in test_srt_writer.py pass)
    - `.planning/phases/05-srt-generation-module/05-VALIDATION.md` — file to delete after VERIFICATION.md is created
  </read_first>

  <action>
Create `.planning/phases/05-srt-generation-module/05-VERIFICATION.md` following the same YAML frontmatter + section structure as `10-VERIFICATION.md` (D-05).

Frontmatter (lines 1–7 of the file): phase=05-srt-generation-module, status=passed, verified=2026-04-20, verification_method=inspection + automated, score=4/4.

File body:
```

# Phase 05 Verification Report

**Phase:** 05 - SRT Generation Module
**Status:** ✅ PASSED
**Date:** 2026-04-20

## Must-Have Verification

### Truths Verified

1. ✅ **Output is a valid SRT file using the `srt` library (SRT-01)**
   - `write_srt()` in `gensubtitles/core/srt_writer.py` uses the `srt` library to produce output
   - SRT output is parseable by `srt.parse()` without error
   - Verified by test #3 in 05-UAT.md
   - Evidence: `tests/test_srt_writer.py` (14 unit tests passing)

2. ✅ **Float timestamps correctly converted to SRT timecodes (SRT-02)**
   - `segments_to_srt()` converts Whisper float seconds to `HH:MM:SS,mmm` format
   - Example: `start=0.0, end=3.5` → `00:00:00,000 --> 00:00:03,500`
   - Verified by test #2 in 05-UAT.md (exact string match)
   - Evidence: `tests/test_srt_writer.py`

3. ✅ **SRT file saved to configurable output path (SRT-03)**
   - `write_srt(segments, output_path)` accepts any output path
   - File created at specified location in UTF-8 encoding
   - Verified by test #3 in 05-UAT.md
   - Evidence: `tests/test_srt_writer.py`

4. ✅ **SRT entries preserve original/translated segment text (SRT-04)**
   - `segments_to_srt()` uses `.text` attribute from input segments (original or translated)
   - Leading/trailing whitespace stripped from text
   - Verified by test #2 in 05-UAT.md (text "Hello world" preserved in output)
   - Evidence: `tests/test_srt_writer.py`

### Artifacts Verified

1. ✅ **gensubtitles/core/srt_writer.py**
   - Contains `segments_to_srt()` function (SRT-01, SRT-02, SRT-04)
   - Contains `write_srt()` function (SRT-01, SRT-03)
   - Empty segment list handled without exception (test #4 in 05-UAT.md)

2. ✅ **tests/test_srt_writer.py**
   - 14 unit tests, all passing (see 05-UAT.md)
   - Tests verify exact SRT format output

### Key Links Verified

1. ✅ **write_srt() → srt library → filesystem**
   - SRT file written as UTF-8 text
   - File parseable by `srt.parse()` confirming valid SRT structure (SRT-01)

2. ✅ **segments_to_srt() → timecode conversion**
   - Float seconds converted to `datetime.timedelta` for SRT timecode precision (SRT-02)
   - Output format verified: `1\n00:00:00,000 --> 00:00:03,500\nHello world`
```

After writing the file, delete `.planning/phases/05-srt-generation-module/05-VALIDATION.md` (per D-03: this draft VALIDATION.md with `status: draft` is superseded by the new VERIFICATION.md).
  </action>

  <verify>
    <automated>Test-Path ".planning/phases/05-srt-generation-module/05-VERIFICATION.md" ; (Get-Content ".planning/phases/05-srt-generation-module/05-VERIFICATION.md" | Select-String "status: passed") ; !(Test-Path ".planning/phases/05-srt-generation-module/05-VALIDATION.md")</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/05-srt-generation-module/05-VERIFICATION.md` exists
    - File contains `status: passed` in YAML frontmatter
    - File contains `score: 4/4`
    - File contains all 4 requirement IDs: `SRT-01`, `SRT-02`, `SRT-03`, `SRT-04`
    - File contains `tests/test_srt_writer.py` as evidence citation
    - File contains `05-UAT.md` reference
    - `.planning/phases/05-srt-generation-module/05-VALIDATION.md` no longer exists (deleted per D-03)
  </acceptance_criteria>

  <done>05-VERIFICATION.md exists with status: passed, all 4 SRT-xx IDs present. 05-VALIDATION.md deleted.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `04-VERIFICATION.md` contains `status: passed` and all 5 TRANS-xx IDs
- `05-VERIFICATION.md` contains `status: passed` and all 4 SRT-xx IDs
- `05-VALIDATION.md` is deleted
- No code files were modified (documentation-only phase)
</verification>

<success_criteria>
- Phase 04 has a formal exit record (`04-VERIFICATION.md`, status: passed, score: 5/5)
- Phase 05 has a formal exit record (`05-VERIFICATION.md`, status: passed, score: 4/4)
- Superseded `05-VALIDATION.md` removed from repository
- All 9 requirement IDs (TRANS-01..05, SRT-01..04) recorded as verified in their respective files
</success_criteria>

<output>
After completion, create `.planning/phases/11-retroactive-verification-core-modules/11-02-SUMMARY.md`
</output>
