---
phase: 999.13-subtitle-style-settings
plan: "02"
subsystem: gui
tags: [customtkinter, settings, color-picker, ssa, i18n]

requires:
  - phase: "999.13-01"
    provides: "AppSettings subtitle style fields; write_ssa/convert_srt_to_ssa style parameter"
provides:
  - Settings panel Subtitle Style section (rows 4-8): separator, font family dropdown, font size entry, text color swatch, outline color swatch
  - _on_pick_text_color() and _on_pick_outline_color() OS color picker callbacks
  - _show_settings() populates 4 style fields from current_settings
  - _save_settings() persists all 4 style fields to AppSettings/settings.json
  - _apply_ui_language() relabels all 5 new style labels in en/es
  - SSA generation branch passes style dict to convert_srt_to_ssa()
affects: [pipeline, settings, i18n]

tech-stack:
  added: [tkinter.colorchooser]
  patterns:
    - "Color swatch buttons use fg_color/hover_color = hex string; updated on picker confirm"
    - "style dict passed to convert_srt_to_ssa: fontname/fontsize/primarycolor/outlinecolor"

key-files:
  created: []
  modified:
    - gensubtitles/gui/main.py

key-decisions:
  - "btn_frame moved from row=4 to row=9 to accommodate 5 new style rows"
  - "Color pickers use tkinter.colorchooser.askcolor() — OS native, no extra dependency"
  - "Style dict only passed when self._current_settings is truthy (safe default)"

patterns-established:
  - "Color swatch: CTkButton width=36, text='', fg_color/hover_color set to hex string"
  - "_on_pick_*_color pattern: askcolor() → check result[1] is not None → set var + configure swatch"

requirements-completed:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
  - STYLE-05
  - STYLE-06

duration: 20min
completed: 2026-04-13
---

# Phase 999.13 Plan 02 Summary

**Subtitle Style settings section added to GUI — font family, font size, text color, outline color controls wired end-to-end from settings panel through AppSettings to SSA generation.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-04-13
- **Tasks:** 3 completed (including human-verify checkpoint: approved)
- **Files modified:** 1

## Accomplishments

### Task 1 — _STRINGS + _build_settings_panel()
Added 5 new i18n keys to both `"en"` and `"es"` _STRINGS dicts. Added rows 4-8 to `_build_settings_panel()`: subtitle style separator label, font family CTkOptionMenu (5 fonts), font size CTkEntry, text color swatch CTkButton, outline color swatch CTkButton. Moved Save/Back btn_frame from row=4 to row=9.

### Task 2 — Wiring (_show_settings, _save_settings, _apply_ui_language, _on_generate)
- Added `_on_pick_text_color()` and `_on_pick_outline_color()` methods using `tkinter.colorchooser.askcolor()`
- `_show_settings()` now populates all 4 style fields from `self._current_settings`
- `_save_settings()` includes all 4 style fields in the `AppSettings()` constructor call
- `_apply_ui_language()` relabels all 5 new style labels
- SSA generation branch builds a style dict from `current_settings` and passes it to `convert_srt_to_ssa()`

### Task 3 — Checkpoint
Human verification: approved.

## Self-Check: PASSED
