---
phase: 999.13-subtitle-style-settings
plan: "01"
subsystem: core
tags: [pysubs2, ssa, settings, tdd]

requires: []
provides:
  - AppSettings with 4 subtitle style fields (subtitle_font_family, subtitle_font_size, subtitle_text_color, subtitle_outline_color)
  - write_ssa() accepts optional style dict, applies fontname/fontsize/primarycolor/outlinecolor to SSAStyle Default
  - convert_srt_to_ssa() accepts optional style dict, forwards to _apply_ssa_style()
  - _hex_to_pysubs2_color() and _apply_ssa_style() helpers in srt_writer
  - 5 new tests in test_srt_writer.py covering style parameter behavior
affects: [999.13-02, gui, pipeline]

tech-stack:
  added: []
  patterns:
    - "Optional style dict passed to write_ssa/convert_srt_to_ssa; applied before subs.save()"
    - "Color round-trips require ASS (.ass) format; SSA (.ssa) drops outlinecolor"

key-files:
  created: []
  modified:
    - gensubtitles/core/settings.py
    - gensubtitles/core/srt_writer.py
    - tests/test_srt_writer.py

key-decisions:
  - "Flat style fields on AppSettings (subtitle_font_family, subtitle_font_size, subtitle_text_color, subtitle_outline_color) with pysubs2 SSA defaults"
  - "ASS (.ass) format required to round-trip outlinecolor; SSA (.ssa) maps it to TertiaryColour which doesn't deserialize back — test_write_ssa_custom_colors uses .ass extension"
  - "_apply_ssa_style() helper shared by write_ssa and convert_srt_to_ssa to avoid duplication"

patterns-established:
  - "Style dict keys: fontname (str), fontsize (int), primarycolor (hex str), outlinecolor (hex str)"
  - "_hex_to_pysubs2_color('#RRGGBB') -> pysubs2.Color(r, g, b, 0)"

requirements-completed:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
  - STYLE-05

duration: 15min
completed: 2026-04-13
---

# Phase 999.13 Plan 01 Summary

**Backend style layer complete — AppSettings stores 4 subtitle style fields; write_ssa() and convert_srt_to_ssa() apply them to pysubs2 SSAStyle Default; 26/26 tests pass.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-04-13
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments

### Task 1 — AppSettings style fields
Added `subtitle_font_family`, `subtitle_font_size`, `subtitle_text_color`, `subtitle_outline_color` after `libretranslate_api_key`. All defaults match pysubs2's implicit SSAStyle values (Arial, 20, #FFFFFF, #000000). `load_settings()` and `save_settings()` needed no changes.

### Task 2 — write_ssa() and convert_srt_to_ssa() style parameter (TDD)
- Added `_hex_to_pysubs2_color(hex: str) -> pysubs2.Color`
- Added `_apply_ssa_style(subs, style)` shared helper
- `write_ssa()` and `convert_srt_to_ssa()` now accept `style: dict | None = None`
- 5 new tests written RED-first; all 5 pass GREEN; all 26 test_srt_writer tests pass

## Issues

- SSA (`.ssa`) format does not preserve outlinecolor through round-trip (pysubs2 maps it to `TertiaryColour` which doesn't deserialize). `test_write_ssa_custom_colors` uses `.ass` extension to properly verify color behavior.

## Self-Check: PASSED
