---
phase: 999.19-configurable-config-json-file-location
plan: "02"
subsystem: gui
tags: [settings-panel, i18n, customtkinter, config-path]

requires:
  - phase: 999.19-01
    provides: "settings_path() with GENSUBTITLES_CONFIG env var support"
provides:
  - "Config file path display row in Settings panel (row=9)"
  - "Open Folder button to open config file directory in system explorer"
  - "i18n strings for config_path_lbl and open_config_folder_btn in EN and ES"
  - "Path label refresh every time Settings panel is opened"
  - "Label/button text updates when UI language is switched"
affects: [gui]

tech-stack:
  added: []
  patterns: ["lazy import of settings_path() inside _show_settings() to always reflect current env var"]

key-files:
  created: []
  modified:
    - gensubtitles/gui/main.py

key-decisions:
  - "Config path row placed at row=9; Save/Back btn_frame shifted to row=10"
  - "Open Folder button uses same cross-platform subprocess.Popen pattern as existing _show_success()"
  - "Path refresh via lazy import in _show_settings() ensures env var changes are always reflected"

patterns-established:
  - "Settings panel row pattern: label col=0, value col=1, action button col=2"

requirements-completed: []

duration: 8min
completed: 2026-04-13
---

# Plan 999.19-02: Config path row in Settings panel

**Settings panel now shows the active config file path with an "Open Folder" button, giving users visibility into which config is in use (especially when `GENSUBTITLES_CONFIG` is set).**

## Performance

- **Duration:** ~8 min
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Added `config_path_lbl` and `open_config_folder_btn` keys to `_STRINGS_EN` and `_STRINGS_ES`
- Inserted config path row at row=9 in `_build_settings_panel()` with:
  - `_lbl_config_path_label` — "Config file:" / "Archivo de config:"
  - `_lbl_config_path_value` — resolved path (updated each time panel opens)
  - `_btn_open_config_folder` — "Open Folder" / "Abrir carpeta" button using cross-platform subprocess.Popen
- Shifted Save/Back btn_frame from row=9 → row=10
- Added path refresh in `_show_settings()` via lazy import of `settings_path()`
- Added label/button text refresh in language-switch section after `_lbl_outline_color`

## Test Results

```
104 passed (all non-e2e, non-translator tests) — no regressions
i18n keys verified: _STRINGS['en'] and _STRINGS['es'] both contain config_path_lbl and open_config_folder_btn
```

## Self-Check: PASSED

- [x] `_lbl_config_path_label` appears 3+ times in gui/main.py (build, show, refresh)
- [x] `_btn_open_config_folder` appears 3+ times in gui/main.py (build, show, refresh)
- [x] `btn_frame.grid(row=10` — Save/Back correctly shifted
- [x] i18n keys present in both language dicts
- [x] No regressions in test suite
