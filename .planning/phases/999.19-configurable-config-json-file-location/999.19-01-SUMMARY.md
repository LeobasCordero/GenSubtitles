---
phase: 999.19-configurable-config-json-file-location
plan: "01"
subsystem: core
tags: [settings, env-var, configuration, pytest]

requires: []
provides:
  - "GENSUBTITLES_CONFIG env var support in settings_path()"
  - "Regression tests for env var override in load_settings and save_settings"
affects: [gui, cli, api]

tech-stack:
  added: []
  patterns: ["env var checked in settings_path() before platformdirs default"]

key-files:
  created: []
  modified:
    - gensubtitles/core/settings.py
    - tests/test_settings.py

key-decisions:
  - "Env var check lives in settings_path() only — load_settings() and save_settings() pick it up automatically"
  - "Used os.environ.get() with local import inside function to stay consistent with existing platformdirs import style"

patterns-established:
  - "settings_path() as the single resolution point for config file path — all callers benefit from env var override without changes"

requirements-completed: []

duration: 5min
completed: 2026-04-13
---

# Plan 999.19-01: GENSUBTITLES_CONFIG env var support

**`settings_path()` now checks `GENSUBTITLES_CONFIG` env var before falling back to the platformdirs default, enabling all three entry points to use a custom config file path transparently.**

## Performance

- **Duration:** ~5 min
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Modified `settings_path()` in `gensubtitles/core/settings.py` to check `GENSUBTITLES_CONFIG` env var first
- Added 3 regression tests to `tests/test_settings.py`:
  - `test_settings_path_respects_env_var` — verifies path resolution
  - `test_load_settings_uses_env_var_path` — verifies load path
  - `test_save_settings_uses_env_var_path` — verifies save path + parent dir creation

## Test Results

```
9 passed in 0.57s
```

## Self-Check: PASSED

- [x] `os.environ.get("GENSUBTITLES_CONFIG")` present in `settings.py`
- [x] All 9 tests pass (6 existing + 3 new)
- [x] No other code changes required — `load_settings()` and `save_settings()` use `settings_path()` automatically
