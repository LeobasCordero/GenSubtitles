---
status: complete
phase: 999.1-gui-interface
source: [999.1-01-SUMMARY.md, 999.1-02-SUMMARY.md]
started: 2026-04-10T00:00:00Z
updated: 2026-04-10T15:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Window Opens via CLI
expected: Run `gensubtitles gui` in a terminal. A desktop window titled "GenSubtitles" appears. No crash or traceback.
result: pass
note: Opened via `python -c "from gensubtitles.gui.main import main; main()"`

### 2. All 6 Input Controls Visible
expected: The window shows exactly these labelled rows — Input video, Output SRT, Model size (dropdown), Device (dropdown), Source language, Target language — plus a "Generate Subtitles" button at the bottom.
result: pass

### 3. Browse Buttons Open File Pickers
expected: Clicking "Browse…" opens a video file-open dialog. Clicking "Save as…" opens a .srt save dialog. Choosing a path populates the field.
result: pass

### 4. Validation — Missing Input
expected: Leave Input video empty, fill Output SRT, click Generate. Modal error: "Missing input — Please select an input video file."
result: pass

### 5. Validation — Missing Output
expected: Fill Input video, leave Output SRT empty, click Generate. Modal error: "Missing output — Please choose an output .srt path."
result: pass

### 6. Progress Bar and Stage Labels During Generation
expected: Fill in real video + output path, click Generate. Button disables, progress bar appears, stage labels cycle every ~2.5s.
result: pass

### 7. Success — SRT Saved and Open Folder Button
expected: After successful generation, progress bar disappears, stage label shows "✓ Done", "Open Folder" button appears, .srt file exists in output folder.
result: pass

### 8. Error Feedback — Bad Input File
expected: Point Input video at a non-video file, click Generate. Modal error popup appears, Generate button re-enables, progress bar disappears.
result: pass

### 9. Standalone Script Entry Point
expected: Run `gensubtitles-gui` directly in a terminal (after `pip install -e .` or equivalent). The same GenSubtitles window opens — identical to the `gensubtitles gui` subcommand.
result: pass

### 10. Window Close Shuts Down Server
expected: Open the window (server starts in background), then close it using the ✕ button. The window closes cleanly. The terminal should not hang. The embedded Uvicorn server stops (no lingering `python` process holding port 8000).
result: pass

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
