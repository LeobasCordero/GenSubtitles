---
plan: 999.29-02
status: complete
completed: 2026-04-18
---

## Summary

Rewrote `_build_ui()` and `_build_stepper_section()` in `gensubtitles/gui/main.py` to implement the three-panel vertical layout. Added `_on_stepper_switch_changed()`, `_log()` helper, and updated `_refresh_stepper_state()` with step-mode guard.

## Key Files

### Modified
- `gensubtitles/gui/main.py` — New 3-panel `_build_ui()`, new `_build_stepper_section()`, new methods

## Changes Made

- `self.minsize(580, 480)` → `self.minsize(1100, 600)`
- Added `self._stepper_switch_var = ctk.BooleanVar(value=False)` and `self._stepper_phase_started` to `__init__`
- Replaced flat `self._frame` grid layout with `self._panels_frame` + 3 CTkFrame panels: `_files_frame`, `_process_frame`, `_status_frame`
- Removed old `self._frame = ctk.CTkFrame(...)` assignment
- New `_build_stepper_section()` populates `_status_frame` with CTkSwitch, work_dir row (hidden by default), 4-stage horizontal flow, dynamic action button, progress bar, stage label, log textbox, clear-work btn, cancel btn
- Added `_on_stepper_switch_changed()` — shows/hides work_dir row, updates button text
- Added `_log()` helper — appends messages to `_log_textbox` with auto-scroll
- Updated `_refresh_stepper_state()` — early-return guard when switch is OFF

## Verification

```
✓ syntax OK
✓ _panels_frame, _files_frame, _status_frame, _switch_stepper, _log_textbox present
✓ minsize(1100, 600) in source
✓ self._frame = ctk.CTkFrame NOT in source
```

## Commits

- `1a3624e` feat(999.29-02): rewrite _build_ui() and _build_stepper_section() for 3-panel layout
