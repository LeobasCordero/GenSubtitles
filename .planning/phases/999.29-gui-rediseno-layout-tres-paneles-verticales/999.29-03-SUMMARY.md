---
plan: 999.29-03
status: complete
completed: 2026-04-18
---

## Summary

Updated `_apply_ui_language()` to configure all new panel title widgets and switch label. Added `_log()` companion calls at all 15 activity sites: 13 `_stage_label.configure` call sites + 2 step lifecycle sites (`_run_step_in_bg` start, `_on_step_success` completion).

## Key Files

### Modified
- `gensubtitles/gui/main.py` — `_apply_ui_language()` extended, `_log()` routing added, `_run_step_in_bg` and `_on_step_success` updated

## Changes Made

### _apply_ui_language() additions (after `_btn_browse_output.configure`):
- `self._lbl_files_title.configure(text=s("panel_files_title"))`
- `self._lbl_process_title.configure(text=s("panel_process_title"))`
- `self._lbl_status_title.configure(text=s("panel_status_title"))`
- `self._switch_stepper.configure(text=s("stepper_switch_lbl"))`
- `self._lbl_work_dir.configure(text=s("work_dir_lbl"))`
- `self._btn_clear_work.configure(text=s("clear_work_btn"))`
- Dynamic generate button text guard (only update when switch OFF)

### _log() routing (15 sites):
- All 13 `self._stage_label.configure(text=...)` sites now have companion `self._log()` calls
- `_run_step_in_bg`: logs `⏳ Running step: {step_key}`
- `_on_step_success`: logs `✓ Step {step_key} completed successfully`
- Lambda forms in prefetch background worker updated to `(configure, _log)` tuple expressions

## Verification

```
✓ syntax OK, 15 _log calls
✓ _apply_ui_language updates OK (_lbl_files_title, _lbl_status_title, _switch_stepper)
✓ locale: Pasos iniciales, Modo paso a paso
```

## Commits

- `268edf7` feat(999.29-03): update _apply_ui_language() + add _log() routing at all stage label sites
