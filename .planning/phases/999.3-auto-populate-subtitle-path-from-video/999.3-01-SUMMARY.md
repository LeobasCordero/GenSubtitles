# Phase 999.3 — Plan 01 Summary

**Plan:** 999.3-01 — Add auto-populate to `_browse_input`
**Status:** Complete
**Committed:** 7aa3cfb

## What Was Built

Added 2 lines to `_browse_input` in `gensubtitles/gui/main.py`. When the user selects a video via "Browse…", the Output SRT field is auto-populated with the same path using `.srt` extension — but only if the output field is currently empty.

## Key Files

### Modified
- `gensubtitles/gui/main.py` — `_browse_input` method extended with conditional auto-populate

## Implementation Details

**Change (inside `if path:` block of `_browse_input`):**
```python
if not self._output_var.get():
    self._output_var.set(str(Path(path).with_suffix(".srt")))
```

- D-01 honored: fires only inside `_browse_input` (browse-only trigger)
- D-02 honored: only sets `_output_var` when `.get()` returns `""` (empty check)
- D-03 honored: `Path(path).with_suffix(".srt")` exact formula
- `Path` was already imported — no new imports needed

## Verification

- Syntax check: `python -c "import ast; ast.parse(...)"` → OK
- Lines present in file at correct location (inside `if path:`, before `_browse_output`)

## Self-Check: PASSED
